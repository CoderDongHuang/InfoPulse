# InfoPulse 开发步骤详解

> **定位**：本文档是《架构设计说明书》（需求.md）的配套执行手册。不写代码，但说清楚每一步创建什么文件、每个文件干什么。
> **使用方式**：按 Phase 顺序执行，每完成一步打勾 ☑️。
> **文档版本**：v1.0
> **最后更新**：2026-07-05

---

## Phase 0：环境攻坚（Docker + Playwright 生存验证）

> **目标**：在写任何业务代码之前，先确认 Playwright + Chromium 在 Docker 里能跑起来。

### Step 0.1 —— 创建项目根目录结构

```
InfoPulse/                          # 项目根目录（Git 仓库根）
├── frontend/                       # 前端项目（Phase 1 才创建）
├── backend/                        # 后端项目（现在创建）
├── docs/                           # 文档
│   └── screenshots/                # 产品截图（后期用）
├── docker-compose.yml              # 后期才写，Phase 6
├── .gitignore
├── README.md                       # Phase 6 才写
└── LICENSE                         # MIT（Phase 6 才加）
```

**本步创建**：

| 操作 | 路径 | 说明 |
|---|---|---|
| 新建文件夹 | `InfoPulse/backend/` | 后端根目录 |
| 新建文件夹 | `InfoPulse/docs/` | 文档目录 |
| 新建文件夹 | `InfoPulse/docs/screenshots/` | 截图目录 |
| 新建文件 | `InfoPulse/.gitignore` | Git 忽略规则 |

**`.gitignore` 要忽略的内容**：

```
# Python
__pycache__/
*.pyc
.venv/
venv/
.env

# Node
node_modules/
dist/

# Docker
pgdata/

# IDE
.vscode/
.idea/

# 爬虫插件（物理隔离 —— 用户自行下载的不纳入版本控制）
backend/app/services/crawler/plugins/*.py

# 系统文件
.DS_Store
Thumbs.db
```

---

### Step 0.2 —— 创建后端 Python 项目骨架

**本步创建**：

| 操作 | 路径 | 说明 |
|---|---|---|
| 新建文件 | `backend/requirements.txt` | Python 依赖清单（完整版，含 Playwright） |
| 新建文件 | `backend/requirements.render.txt` | Python 依赖清单（演示站轻量版，不含 Playwright） |
| 新建文件 | `backend/.env.example` | 环境变量模板 |
| 新建文件 | `backend/Dockerfile` | 完整版 Docker 镜像（含 Chromium） |
| 新建文件 | `backend/Dockerfile.render` | 演示站 Docker 镜像（不含 Chromium） |
| 新建文件 | `backend/test_playwright.py` | Playwright 冒烟测试脚本 |

**各文件职责**：

**`backend/requirements.txt`**（完整版依赖清单）：
- fastapi、uvicorn、pydantic —— Web 框架
- sqlalchemy、asyncpg、alembic —— 数据库
- redis —— 缓存和任务队列
- python-jose、passlib —— JWT 认证
- langchain、openai —— AI 调度
- httpx、beautifulsoup4 —— 轻量爬虫
- playwright、playwright-stealth —— 重量爬虫
- psutil —— 内存监控熔断

**`backend/requirements.render.txt`**（演示站轻量版）：
- 和上面一样，但**移除** playwright、playwright-stealth、psutil
- 移除原因：Render 免费层只有 512MB，能省 50-80MB 是 50-80MB

**`backend/.env.example`**（环境变量模板）：
- DATABASE_URL、REDIS_URL —— 数据库连接
- JWT_SECRET_KEY、ACCESS_TOKEN_EXPIRE_MINUTES、REFRESH_TOKEN_EXPIRE_DAYS —— 认证配置
- LLM_API_KEY、LLM_API_BASE、LLM_MODEL —— 大模型配置
- CRAWLER_HEADLESS、CRAWLER_REQUEST_INTERVAL_MIN/MAX —— 爬虫配置
- BROWSER_RESTART_MB —— 浏览器熔断阈值（默认 800）
- DEMO_MODE、CRAWLER_ENABLED —— 演示模式开关

**`backend/Dockerfile`**（完整版 —— 含 Chromium）：
- 基础镜像：`python:3.11-slim-bookworm`
- 安装系统依赖：`libnss3 libnspr4 libatk-bridge2.0-0 libdrm2 libxkbcommon0 libxcomposite1 libxdamage1 libxrandr2 libgbm1 libpango-1.0-0 libcairo2 libasound2 libatspi2.0-0`
- 安装 Python 依赖：`pip install -r requirements.txt`
- 安装 Chromium：`playwright install chromium && playwright install-deps chromium`
- 工作目录：`/app`
- 启动命令：`uvicorn app.main:app --host 0.0.0.0 --port 8000`

**`backend/Dockerfile.render`**（演示站轻量版 —— 不含 Chromium）：
- 基础镜像：`python:3.11-slim-bookworm`
- 只安装轻量系统依赖（不需要 Playwright 的那些库）
- 安装 Python 依赖：`pip install -r requirements.render.txt`
- 无 Playwright 安装步骤
- 其余同上

**`backend/test_playwright.py`**（冒烟测试脚本）：
- 功能：启动 Chromium → 打开百度首页 → 截图保存到 `/tmp/baidu.png` → 关闭浏览器
- 验证点：① 浏览器能启动 ② 能访问外网 ③ 截图成功
- 运行方式：`docker build -t infopulse-test . && docker run --rm infopulse-test python test_playwright.py`

---

### Step 0.3 —— 验证 Playwright 在 Docker 中能正常工作

**操作**：
1. `cd backend`
2. `docker build -t infopulse-test -f Dockerfile .`
3. `docker run --rm infopulse-test python test_playwright.py`
4. 检查输出：应该打印 "Screenshot saved to /tmp/baidu.png"，并且没有 chromium 报错

**验证通过标准**：
- ✅ 容器内 Chromium 正常启动，无 `"Browser not found"` 错误
- ✅ 百度首页成功打开并截图
- ✅ 无 `"libnss3.so"` 或 `"libgbm.so"` 等系统库缺失报错

---

## Phase 1：骨架搭建（前后端框架 + 认证）

> **目标**：前后端项目能跑起来，用户能注册、登录、看到导航栏。

---

### Step 1.1 —— 创建 FastAPI 后端骨架

**本步创建的文件夹**：

```
backend/app/                        # 应用主目录
backend/app/__init__.py
backend/app/main.py                 # FastAPI 入口
backend/app/config.py               # 配置管理
backend/app/dependencies.py         # 依赖注入
backend/app/models/                 # 数据库模型
backend/app/models/__init__.py
backend/app/schemas/                # Pydantic 请求/响应模型
backend/app/schemas/__init__.py
backend/app/api/                    # 路由层
backend/app/api/__init__.py
backend/app/services/               # 业务逻辑层
backend/app/services/__init__.py
backend/app/core/                   # 基础设施
backend/app/core/__init__.py
backend/app/middleware/             # 中间件
backend/app/middleware/__init__.py
backend/alembic/                    # 数据库迁移（后期配置）
backend/tests/                      # 测试
```

**各文件职责**：

**`backend/app/main.py`** —— FastAPI 应用入口：
- 创建 FastAPI 实例，设置 title="InfoPulse API", version="1.0.0"
- 注册 CORS 中间件（允许 localhost:5173 和 Vercel 演示站域名）
- 注册所有路由模块（auth、anti_scam、mouthpiece、timeline）
- 添加启动事件：连接数据库、连接 Redis
- 添加关闭事件：断开数据库、断开 Redis
- 添加健康检查路由 `GET /api/v1/health`：返回 `{"status": "ok", "llm": "available", "crawler": "disabled" if demo else "ready"}`

**`backend/app/config.py`** —— 配置管理：
- 使用 Pydantic `BaseSettings`，自动从 `.env` 文件读取
- 包含的配置项：DATABASE_URL、REDIS_URL、JWT_SECRET_KEY、JWT_ALGORITHM、ACCESS_TOKEN_EXPIRE_MINUTES、REFRESH_TOKEN_EXPIRE_DAYS、LLM_API_KEY、LLM_API_BASE、LLM_MODEL、CRAWLER_HEADLESS、CRAWLER_REQUEST_INTERVAL_MIN、CRAWLER_REQUEST_INTERVAL_MAX、BROWSER_RESTART_MB、DEMO_MODE、CRAWLER_ENABLED

**`backend/app/dependencies.py`** —— 依赖注入：
- `get_db()`：数据库 Session 生成器
- `get_current_user()`：从 JWT Token 解析当前用户，注入到路由中

**`backend/app/core/database.py`** —— 数据库连接：
- 创建 SQLAlchemy async engine（从 `DATABASE_URL`）
- 创建 `AsyncSession` 工厂
- 创建 Base 模型基类
- `init_db()` 函数：启动时自动创建所有表

**`backend/app/core/security.py`** —— JWT 安全：
- `create_access_token(data: dict)`：生成 Access Token（30 分钟过期）
- `create_refresh_token(data: dict)`：生成 Refresh Token（7 天过期）
- `verify_token(token: str)`：验证并解码 Token
- `hash_password(password: str)`：bcrypt 哈希
- `verify_password(plain: str, hashed: str)`：验证密码

**`backend/app/core/redis.py`** —— Redis 连接：
- 创建 Redis 客户端（从 `REDIS_URL`）
- `init_redis()` / `close_redis()` 函数

**`backend/app/core/llm.py`** —— LLM 客户端封装：
- 封装 OpenAI 兼容的 LLM 客户端（从 `LLM_API_KEY`、`LLM_API_BASE`、`LLM_MODEL`）
- 提供 `stream_chat()` 方法：异步生成器，返回流式文本片段

**`backend/app/middleware/cors.py`** —— CORS 中间件：
- 配置允许的来源、方法、头部
- 允许携带 Cookie（`allow_credentials=True`）

**`backend/app/models/user.py`** —— 用户表模型：
- 表名：`users`
- 字段：id (UUID, PK)、username (VARCHAR 50, UNIQUE)、email (VARCHAR 100, UNIQUE)、password_hash (VARCHAR 255)、avatar_url (VARCHAR 500)、is_active (BOOLEAN)、created_at (DATETIME)、updated_at (DATETIME)

**`backend/app/models/analysis_history.py`** —— 分析历史表模型：
- 表名：`analysis_histories`
- 字段：id (UUID, PK)、user_id (UUID, FK→users)、module (VARCHAR 20)、input_params (JSON)、output_result (JSON)、status (VARCHAR 20)、error_message (TEXT)、created_at (DATETIME)

**`backend/app/models/saved_result.py`** —— 收藏结果表模型：
- 表名：`saved_results`
- 字段：id (UUID, PK)、user_id (UUID, FK→users)、history_id (UUID, FK→analysis_histories)、note (VARCHAR 200)、created_at (DATETIME)

**`backend/app/schemas/auth.py`** —— 认证相关 Pydantic 模型：
- `UserRegisterRequest`：username、email、password
- `UserLoginRequest`：username、password
- `TokenResponse`：access_token、refresh_token、token_type
- `UserResponse`：id、username、email、avatar_url、created_at

**`backend/app/api/auth.py`** —— 认证路由：
- `POST /api/v1/auth/register`：注册（校验用户名/邮箱唯一性 → 哈希密码 → 写入数据库 → 返回 Token）
- `POST /api/v1/auth/login`：登录（校验用户名密码 → 生成 JWT → 返回 Token）
- `POST /api/v1/auth/refresh`：刷新 Token（验证 Refresh Token → 生成新的 Access Token）
- `GET /api/v1/auth/me`：获取当前用户信息（从 JWT 解析 user_id → 查数据库 → 返回）
- `PUT /api/v1/auth/me`：更新用户信息

**`backend/app/services/auth_service.py`** —— 认证业务逻辑：
- `register_user()`：校验 → 创建用户 → 返回 Token
- `login_user()`：校验 → 生成 Token → 返回
- `get_user_by_id()`：查询用户

---

### Step 1.2 —— 创建 Vue 3 前端骨架

**本步创建的文件夹和文件**：

```
frontend/                           # 前端根目录
├── public/
│   └── favicon.ico                 # 网站图标（先用占位图）
├── src/
│   ├── assets/
│   │   ├── logo.png                # Logo 图片（先放占位图）
│   │   └── styles/
│   │       ├── variables.css       # CSS 变量（色彩系统）
│   │       ├── global.css          # 全局样式（毛玻璃、字体、圆角）
│   │       └── transitions.css     # Vue Router 过渡动画
│   ├── components/
│   │   ├── layout/
│   │   │   ├── AppHeader.vue       # 顶部导航栏
│   │   │   ├── AppFooter.vue       # 底部状态栏
│   │   │   └── AppLayout.vue       # 全局布局容器（Header + RouterView + Footer）
│   │   ├── common/
│   │   │   ├── LoadingOverlay.vue  # 全局加载蒙层
│   │   │   ├── SseTextStream.vue   # SSE 流式文本渲染（打字机效果）
│   │   │   ├── EmptyState.vue      # 空状态占位图
│   │   │   └── ErrorRetry.vue      # 错误重试组件
│   │   └── auth/
│   │       └── LoginCard.vue       # 登录/注册卡片
│   ├── views/
│   │   ├── HomeView.vue            # 首页（Hero + 三大功能卡片）
│   │   └── AuthView.vue            # 登录/注册页
│   ├── router/
│   │   └── index.ts                # 路由配置 + 导航守卫
│   ├── stores/
│   │   ├── user.ts                 # 用户登录状态（Pinia）
│   │   └── app.ts                  # 全局配置（主题、加载态）
│   ├── api/
│   │   ├── request.ts              # Axios 实例 + 请求/响应拦截器
│   │   └── auth.ts                 # 认证接口封装
│   ├── utils/
│   │   ├── sse.ts                  # SSE 连接工具
│   │   └── clipboard.ts            # 剪贴板工具
│   ├── App.vue                     # 根组件
│   └── main.ts                     # 入口文件（创建 App、注册 Router/Pinia/ElementPlus）
├── index.html
├── vite.config.ts                  # Vite 配置（代理 /api 到 localhost:8000）
├── tsconfig.json
├── package.json
└── .env.development                # 开发环境变量
```

**各文件职责**：

**`frontend/src/assets/styles/variables.css`** —— CSS 色彩变量：
- 定义 `:root` 下的所有 CSS 自定义属性
- 包括：`--color-primary`(#409EFF)、`--color-accent`(#6B46C1)、`--bg-page`、`--bg-card`、`--text-primary`、`--text-regular`、`--text-secondary`、`--border-color`、`--color-success`、`--color-warning`、`--color-danger`
- 暗黑模式变量放在 `[data-theme="dark"]` 下

**`frontend/src/assets/styles/global.css`** —— 全局样式：
- 重置浏览器默认样式（`*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }`）
- 设置 `body` 背景色、字体栈、行高
- 毛玻璃卡片基础类 `.glass-card`
- 全局滚动条美化

**`frontend/src/assets/styles/transitions.css`** —— 路由过渡动画：
- `.fade-enter-active` / `.fade-leave-active`：300ms 淡入淡出
- `.slide-enter-active` / `.slide-leave-active`：300ms 滑动

**`frontend/src/components/layout/AppHeader.vue`** —— 顶部导航栏：
- 三段式布局：左（Logo + "InfoPulse"） + 中（导航菜单：首页、避坑雷达▾、嘴替生成▾、吃瓜时间线▾） + 右（未登录：登录/注册按钮；已登录：铃铛图标 + 头像下拉菜单）
- 导航项使用 `<router-link>`，当前路由高亮（底部 2px 蓝线）
- 悬停效果：上浮 2px + 颜色过渡
- 下拉子菜单：悬停展开，毛玻璃背景
- 响应式：<768px 时显示汉堡菜单图标（先用 Element Plus 的 `el-drawer` 实现）

**`frontend/src/components/layout/AppFooter.vue`** —— 底部状态栏：
- 左侧：面包屑导航（当前页面路径）
- 右侧：系统时间（`new Date().toLocaleString()`）、网络状态指示器、版权信息 "© 2026 InfoPulse"

**`frontend/src/components/layout/AppLayout.vue`** —— 全局布局容器：
- 包含：AppHeader + `<router-view>`（带 `<transition>` 动画） + AppFooter
- 主内容区 `min-height: calc(100vh - 120px)`

**`frontend/src/components/common/LoadingOverlay.vue`** —— 全局加载蒙层：
- Props：`visible: boolean`、`text: string`（默认 "加载中..."）
- 显示：半透明遮罩 + 居中旋转加载图标 + 文字
- 用于爬虫分析等待时覆盖整个页面

**`frontend/src/components/common/SseTextStream.vue`** —— 流式文本渲染：
- Props：`text: string`（不断追加的文本）
- 功能：接收 SSE 推送的文本片段，模拟打字机效果逐字显示
- 自动滚动到底部

**`frontend/src/components/common/EmptyState.vue`** —— 空状态占位：
- Props：`icon: string`、`title: string`、`description: string`
- 显示插画图标 + 标题 + 描述文字

**`frontend/src/components/common/ErrorRetry.vue`** —— 错误重试：
- Props：`message: string`、`onRetry: Function`
- 显示错误图标 + 错误信息 + "重试"按钮

**`frontend/src/components/auth/LoginCard.vue`** —— 登录/注册卡片：
- 毛玻璃卡片（400px 宽，居中）
- 两个 Tab：登录 / 注册
- 登录 Tab：用户名输入框 + 密码输入框 + 登录按钮
- 注册 Tab：用户名 + 邮箱 + 密码 + 确认密码 + 注册按钮
- 表单校验：用户名≥3字符、邮箱格式、密码≥6字符、确认密码一致
- 调用 `api/auth.ts` 接口，成功后更新 Pinia `userStore`，跳转首页

**`frontend/src/views/HomeView.vue`** —— 首页：
- Hero 区域：大标题 "穿透信息迷雾，洞察真实情绪" + 副标题 + "立即体验" 按钮（跳转 `/anti-scam`）
- 三大功能卡片（CSS Grid 3 列，响应式缩为 1 列）：
  - 避坑雷达：图标 + "多平台水军识别，买到真正好货" + "立即使用 →"
  - 嘴替生成：图标 + "AI 帮你把心里话，说成爆款" + "立即使用 →"
  - 吃瓜时间线：图标 + "碎片信息结构化，吃瓜吃明白" + "立即使用 →"
- 底部：简单的统计数字（"已分析 0 条内容，已生成 0 篇文案" —— 先静态，后期接 API）

**`frontend/src/views/AuthView.vue`** —— 登录/注册页：
- 全屏居中布局（背景渐变色）
- 使用 `LoginCard` 组件
- 无导航栏（隐藏 AppHeader）

**`frontend/src/router/index.ts`** —— 路由配置：
- 路由表：
  - `/` → HomeView（首页）
  - `/auth` → AuthView（登录/注册）
  - `/anti-scam` → AntiScamView（避坑雷达，Phase 2 创建）
  - `/mouthpiece` → MouthpieceView（嘴替生成，Phase 3 创建）
  - `/timeline` → TimelineView（吃瓜时间线，Phase 4 创建）
- 导航守卫 `router.beforeEach`：
  - 如果目标路由需要认证（`meta.requiresAuth: true`）且用户未登录 → 跳转 `/auth`，并记住目标路径（`redirect` 参数）
  - 如果已登录用户访问 `/auth` → 跳转首页

**`frontend/src/stores/user.ts`** —— 用户状态（Pinia）：
- State：`token`（Access Token，存内存不持久化）、`userInfo`（用户信息对象）、`isLoggedIn`（计算属性）
- Actions：`login(username, password)`、`register(username, email, password)`、`logout()`、`refreshToken()`、`fetchUserInfo()`
- 登录成功后将 token 存 Pinia，同时调用 `fetchUserInfo()` 获取用户详情

**`frontend/src/stores/app.ts`** —— 全局应用状态（Pinia）：
- State：`theme`（'light' | 'dark'）、`globalLoading`、`headerTitle`
- Actions：`toggleTheme()`、`setLoading()`、`setHeaderTitle()`

**`frontend/src/api/request.ts`** —— Axios 实例：
- 创建 Axios 实例，baseURL = `/api/v1`（开发环境通过 Vite proxy 转发到 localhost:8000）
- 请求拦截器：自动在请求头附加 `Authorization: Bearer <token>`
- 响应拦截器：
  - 401 错误 → 尝试 refreshToken() → 失败则跳转 `/auth`
  - 500 错误 → 全局 Toast 提示 "服务器开小差了"
  - 网络错误 → Toast "网络连接异常"

**`frontend/src/api/auth.ts`** —— 认证接口：
- `register(data)` → POST `/auth/register`
- `login(data)` → POST `/auth/login`
- `refreshToken()` → POST `/auth/refresh`
- `getMe()` → GET `/auth/me`
- `updateMe(data)` → PUT `/auth/me`

**`frontend/src/utils/sse.ts`** —— SSE 连接工具：
- `createSSEConnection(url, callbacks)` 函数：
  - 创建 `EventSource` 连接
  - `callbacks.onProgress(data)`：处理进度事件
  - `callbacks.onChunk(text)`：处理文本片段
  - `callbacks.onResult(data)`：处理最终结果
  - `callbacks.onWarning(data)`：处理警告
  - `callbacks.onError(data)`：处理错误
  - `callbacks.onPing()`：心跳处理（重置超时计时器）
- 30 秒无任何事件自动断开并回调 `onTimeout`
- 返回 `{ close() }` 方法用于手动断开

**`frontend/src/utils/clipboard.ts`** —— 剪贴板工具：
- `copyToClipboard(text: string)`：复制文本到剪贴板，成功/失败均 Toast 提示

**`frontend/vite.config.ts`** —— Vite 配置：
- 开发代理：`/api` → `http://localhost:8000`
- Element Plus 按需导入插件（可选）

**`frontend/.env.development`** —— 开发环境变量：
- `VITE_API_BASE_URL=http://localhost:8000`

---

### Step 1.3 —— 验证前后端联通

**操作**：
1. 启动 PostgreSQL 和 Redis（可以用 Docker 单独启动：`docker run -d --name pg -p 5432:5432 -e POSTGRES_PASSWORD=infopulse postgres:15-alpine` 和 `docker run -d --name redis -p 6379:6379 redis:7-alpine`）
2. `cd backend && pip install -r requirements.txt && uvicorn app.main:app --reload`
3. `cd frontend && npm install && npm run dev`
4. 浏览器打开 `http://localhost:5173`
5. 验证：访问首页 → 点击登录 → 注册账号 → 登录成功 → 看到导航栏显示用户头像

**验证通过标准**：
- ✅ 前端首页正常渲染（Hero + 三大功能卡片）
- ✅ 注册成功，数据库 `users` 表有记录
- ✅ 登录成功，导航栏右侧显示用户头像
- ✅ 登录后刷新页面，仍然保持登录状态（Pinia + refreshToken）
- ✅ 未登录访问 `/anti-scam` 自动跳转 `/auth`
- ✅ 后端 `/api/v1/health` 返回 `{"status": "ok"}`

---

## Phase 2：避坑雷达（爬虫 + AI 分析 + 前端页面）

> **目标**：第一个完整核心功能——用户输入关键词 → 爬虫抓取 → AI 分析 → 流式展示结果。

---

### Step 2.1 —— 创建爬虫模块（知乎轻量爬虫先行）

**本步创建**：

```
backend/app/services/crawler/
├── __init__.py                    # 爬虫注册表 + 导出
├── base.py                        # BaseCrawler 抽象基类 + RawPost/RawComment 数据结构
├── zhihu.py                       # 知乎爬虫（httpx，Phase 2 先做这个）
├── weibo.py                       # 微博爬虫（Phase 2 后期做）
├── browser_manager.py             # Playwright 浏览器单例管理器（Phase 2 后期做）
├── stealth_patcher.py             # playwright-stealth 集成（Phase 2 后期做）
└── plugins/
    └── README.md                  # 第三方插件安装说明
```

**各文件职责**：

**`backend/app/services/crawler/base.py`** —— 抽象基类：
- `RawPost` dataclass：post_id、title、content、author、publish_time、url、platform
- `RawComment` dataclass：comment_id、post_id、content、author、like_count、publish_time
- `BaseCrawler` 抽象类：
  - `platform_name: str` 类属性
  - `async search(keyword, max_items) -> List[RawPost]` 抽象方法
  - `async get_comments(post_id, max_items) -> List[RawComment]` 抽象方法
  - `is_available() -> bool` 抽象方法

**`backend/app/services/crawler/zhihu.py`** —— 知乎爬虫：
- 继承 `BaseCrawler`
- `search()`：调用知乎搜索 API（`/api/v4/search_v3`），解析 JSON 返回，提取帖子标题、摘要、URL、作者、发布时间
- `get_comments()`：调用知乎回答 API，解析评论列表
- 使用 `httpx.AsyncClient`，设置合理的 User-Agent 和请求间隔
- 每次请求间隔 1-3 秒随机

**`backend/app/services/crawler/__init__.py`** —— 爬虫注册表：
- `CRAWLER_REGISTRY` 字典：`{"zhihu": ZhihuCrawler, "weibo": WeiboCrawler}`
- 尝试导入 `plugins/` 目录下的第三方爬虫，自动注册
- `get_crawler(platform_name) -> BaseCrawler` 工厂函数
- **⚠️ Playwright 导入防御**：在文件顶部用 `try/except ImportError` 包裹 `from playwright.async_api import ...`：
  - 导入成功 → `CRAWLER_ENABLED = True`
  - 导入失败（演示站 Dockerfile.render 没装 playwright）→ `CRAWLER_ENABLED = False`，且任何调用爬虫的方法返回明确的 `"Crawler not available in demo mode"` 错误，而非让 Python 抛出 `ModuleNotFoundError` 这个会让用户困惑的报错

**`backend/app/services/crawler/plugins/README.md`** —— 插件说明：
- 说明本目录用于存放第三方爬虫插件
- 给出安装步骤（下载 .py 文件 → 放入本目录 → 重启）
- 提供下载链接（Gist/网盘地址）

---

### Step 2.2 —— 创建 AI 分析模块

**本步创建**：

```
backend/app/services/analyzer/
├── __init__.py
├── anti_scam.py                   # 水军识别 + 痛点提炼
├── mouthpiece.py                  # 文案生成（Phase 3）
└── timeline.py                    # 时间线梳理（Phase 4）

backend/app/services/cleaner.py    # 数据清洗器

backend/app/services/prompts/
├── __init__.py
├── loader.py                      # Prompt 热加载器
└── templates/
    ├── anti_scam_system.txt       # 避坑雷达 System Prompt
    └── anti_scam_user.txt         # 避坑雷达 User Prompt 模板
```

**各文件职责**：

**`backend/app/services/cleaner.py`** —— 数据清洗器：
- `clean_html(html: str) -> str`：去除 HTML 标签
- `deduplicate(posts: List[RawPost]) -> List[RawPost]`：根据标题相似度去重
- `truncate_text(text: str, max_chars: int = 4000) -> str`：智能截断（在完整句子边界处截断，保留上下文）
- `clean_posts(posts: List[RawPost]) -> str`：将帖子列表清洗后拼接成一个适合喂给 LLM 的文本块（格式：`[平台] 标题：xxx\n内容：xxx\n---\n`）

**`backend/app/services/prompts/loader.py`** —— Prompt 热加载器：
- `load_prompt(name: str) -> str`：从 `templates/{name}.txt` 读取内容
- 每次调用都重新读取文件（实现热更新——不需要重启服务）
- 支持 `str.format()` 变量替换：`load_prompt("anti_scam_user").format(keyword="xxx", posts="xxx")`

**`backend/app/services/prompts/templates/anti_scam_system.txt`** —— System Prompt：
- 角色：资深消费者权益保护专家
- 任务：分析以下来自多个平台的用户评论，识别水军评论，提炼真实用户痛点
- 水军判断标准：① 模板化/重复性语言 ② 过度使用感叹号和夸张修饰 ③ 缺乏具体使用细节 ④ 发布时间集中在短时间内 ⑤ 账号信息可疑
- 输出要求：严格按照 JSON 格式输出（定义好字段）

**`backend/app/services/prompts/templates/anti_scam_user.txt`** —— User Prompt 模板：
- 模板变量：`{keyword}`、`{posts_text}`
- 内容："请分析以下关于'{keyword}'的互联网公开评论数据：\n\n{posts_text}\n\n请输出 JSON 格式的分析结果。"

**`backend/app/services/analyzer/anti_scam.py`** —— 避坑雷达分析器：
- `AntiScamAnalyzer` 类：
  - `async analyze(keyword, posts, stream_callback)` 方法：
    1. 调用 `cleaner.clean_posts(posts)` 清洗数据
    2. 调用 `prompts.loader` 加载 System + User Prompt
    3. 调用 `llm.stream_chat()` 流式调用大模型
    4. 通过 `stream_callback(chunk)` 逐片回传文本
    5. 流式结束后，尝试用 `PydanticOutputParser` 解析完整 JSON
    6. 解析失败则重试一次（附加错误信息）
    7. 返回结构化的 `AntiScamResult` 对象

---

### Step 2.3 —— 创建后端 API 和 Schema

**本步创建**：

```
backend/app/schemas/anti_scam.py   # 避坑雷达请求/响应 Pydantic 模型
backend/app/api/anti_scam.py       # 避坑雷达 API 路由
```

**各文件职责**：

**`backend/app/schemas/anti_scam.py`** —— 数据模型：
- `AnalyzeRequest`：keyword (str)、platforms (List[str])、max_items (int, 默认 50)
- `WaterArmyComment`：content (str)、reason (str)、confidence (float)
- `AntiScamResult`：risk_score (int, 0-100)、water_army_comments (List[WaterArmyComment])、real_pain_points (List[str])、summary (str)
- `ProgressEvent`：stage (str)、platform (str, 可选)、current (int, 可选)、total (int, 可选)、estimated_wait_s (int, 可选)

**`backend/app/api/anti_scam.py`** —— API 路由：
- `POST /api/v1/anti-scam/analyze`：
  - 需要认证（`Depends(get_current_user)`）
  - 返回 `StreamingResponse`（`media_type="text/event-stream"`）
  - 流式事件生成逻辑：
    1. 检查 Redis 队列，返回 `progress` 事件告知排队位置和预计等待时间
    2. 加入队列，等待轮到当前用户
    3. 遍历 `platforms`，逐个调用对应爬虫的 `search()` + `get_comments()`
    4. 每个平台抓取完成后发送 `progress` 事件（stage="crawling", platform=..., current=..., total=...）
    5. 某个平台抓取失败时发送 `warning` 事件，继续下一个平台
    6. 全部抓取完成后，发送 `progress` 事件（stage="analyzing"）
    7. 调用 `AntiScamAnalyzer.analyze()`，通过回调逐片发送 `chunk` 事件
    8. 分析完成后，发送 `result` 事件（完整 JSON）
    9. 异步写入 `analysis_histories` 表
    10. 每 5 秒无数据时自动发送 `ping` 事件（心跳）
  - 路由需要调用 `get_current_user` 依赖注入获取 user_id
  - 结果写入 `analysis_histories` 表（异步，不阻塞流式响应）

- `GET /api/v1/anti-scam/history`：
  - 需要认证
  - 分页查询当前用户的 `analysis_histories`（module='anti_scam'），按创建时间倒序

- `GET /api/v1/anti-scam/history/{id}`：
  - 需要认证
  - 查询单条记录详情，校验属于当前用户

- `DELETE /api/v1/anti-scam/history/{id}`：
  - 需要认证
  - 删除单条记录，校验属于当前用户

---

### Step 2.4 —— 创建避坑雷达前端页面

**本步创建**：

```
frontend/src/views/anti-scam/
└── AntiScamView.vue               # 避坑雷达主页面

frontend/src/api/antiScam.ts       # 避坑雷达 API 封装
frontend/src/stores/history.ts     # 历史记录状态管理（可选，也可以直接用 api 调用）
```

**各文件职责**：

**`frontend/src/api/antiScam.ts`** —— API 封装：
- `analyzeStream(data, callbacks)`：创建 SSE 连接到 `POST /anti-scam/analyze`，传入回调
- `getHistory(page, pageSize)`：GET `/anti-scam/history`
- `getHistoryDetail(id)`：GET `/anti-scam/history/{id}`
- `deleteHistory(id)`：DELETE `/anti-scam/history/{id}`

**`frontend/src/views/anti-scam/AntiScamView.vue`** —— 避坑雷达页面：
- 布局：左侧输入配置区（30%）+ 右侧结果展示区（70%）
- **左侧输入区**：
  - 关键词输入框（`el-input`，placeholder："输入商品或事件关键词，如'某品牌手机'"）
  - 平台多选框（`el-checkbox-group`）：知乎（默认勾选）、微博、小红书（旁边标注 `🔌 需要插件`，点击弹出安装引导弹窗）
  - 抓取数量滑块（`el-slider`，10-100，默认 50）
  - "开始排雷" 按钮（`el-button`，type="primary"，点击后进入加载状态）
- **右侧结果展示区**（初始为空状态）：
  - 点击分析后，顶部显示实时进度条（`el-progress`），左侧文字显示当前阶段（"正在抓取 知乎 数据..."）
  - 分析完成后显示：
    - **风险仪表盘**：`el-progress` 环形，type="dashboard"，显示风险评分 0-100，颜色根据分数变化（绿<30，黄 30-70，红>70）
    - **痛点标签云**：`el-tag` 数组，不同颜色，`closable=false`
    - **证据列表**：`el-collapse` 折叠面板，每条水军评论占一个 `el-collapse-item`，标题显示评论摘要，展开后显示完整内容 + 判定理由 + 置信度
    - **操作日志面板**：底部 `el-collapse`，显示爬取过程的详细日志（哪个平台成功、哪个降级）
- **状态管理**：
  - `analyzing: boolean`：是否正在分析
  - `progress: {stage, platform, current, total}`：当前进度
  - `result: AntiScamResult | null`：分析结果
  - `logs: string[]`：操作日志
  - `streamText: string`：AI 流式输出的文本（用于打字机效果）
- **交互流程**：
  1. 用户输入关键词 → 选择平台 → 点击"开始排雷"
  2. 按钮变为 Loading 状态，右侧清空上次结果
  3. SSE 连接建立 → 收到 `progress` 事件 → 更新进度条和日志
  4. 收到 `warning` 事件 → 日志面板显示黄色警告
  5. 收到 `chunk` 事件 → `SseTextStream` 组件实时渲染打字机效果
  6. 收到 `result` 事件 → 解析 JSON → 渲染仪表盘 + 标签云 + 证据列表
  7. 收到 `ping` 事件 → 无 UI 变化，仅重置超时计时器
  8. 收到 `error` 事件 → 显示 `ErrorRetry` 组件

---

### Step 2.5 —— 验证避坑雷达端到端流程

**操作**：
1. 确保数据库中有至少一个注册用户
2. 登录后访问 `/anti-scam`
3. 输入关键词 "iPhone 15"，只勾选知乎，点击"开始排雷"
4. 观察：进度条更新 → 流式文本输出 → 结果页渲染

**验证通过标准**：
- ✅ 知乎搜索成功，返回帖子数据
- ✅ SSE 流式传输正常工作，打字机效果流畅
- ✅ AI 分析结果正确解析为 JSON，仪表盘和数据列表正常渲染
- ✅ 分析记录写入 `analysis_histories` 表
- ✅ 历史记录页面可查询到本次分析

---

## Phase 3：互联网嘴替（Prompt 工程 + 文案预览）

> **目标**：第二个核心功能——用户输入大白话 → AI 生成爆款文案 → 小红书风格预览。

---

### Step 3.1 —— 创建嘴替生成模块

**本步创建**：

```
backend/app/services/prompts/templates/
├── mouthpiece_system.txt          # 嘴替生成 System Prompt
└── mouthpiece_user.txt            # 嘴替生成 User Prompt 模板

backend/app/schemas/mouthpiece.py  # 嘴替生成请求/响应 Schema
backend/app/api/mouthpiece.py      # 嘴替生成 API 路由
backend/app/services/analyzer/mouthpiece.py  # 嘴替生成分析器
```

**各文件职责**：

**`backend/app/services/prompts/templates/mouthpiece_system.txt`**：
- 角色：顶级新媒体文案写手，擅长小红书/微博爆款文风
- 四种情绪风格指令字典：
  - `sarcastic`（阴阳怪气）：用优雅的词汇表达不满，话里有话，让读者拍大腿
  - `humorous`（幽默自黑）：用自嘲化解尴尬，让人笑着笑着就哭了
  - `gentle_crazy`（温柔发疯）：表面上语气温和，但内容句句戳心
  - `sharp`（犀利毒舌）：一针见血，不带脏字但杀伤力极强
- 输出格式 JSON：`title`（≤20字）、`content`（3-5段，含 Emoji）、`tags`（5-8个标签）

**`backend/app/services/prompts/templates/mouthpiece_user.txt`**：
- 模板变量：`{scenario}`、`{emotion}`、`{emotion_intensity}`、`{raw_content}`
- 内容：将用户的大白话和情绪参数拼接成指令

**`backend/app/schemas/mouthpiece.py`**：
- `GenerateRequest`：scenario (str)、emotion (str, 枚举值)、emotion_intensity (int, 1-10)、content (str)
- `MouthpieceResult`：title (str)、content (str)、tags (List[str])
- `ScenarioEnum`：职场吐槽、相亲分享、探店测评、生活趣事
- `EmotionEnum`：sarcastic、humorous、gentle_crazy、sharp

**`backend/app/api/mouthpiece.py`** —— API 路由：
- `POST /api/v1/mouthpiece/generate`：SSE 流式返回生成的文案
- `GET /api/v1/mouthpiece/history`：分页查询历史
- `GET /api/v1/mouthpiece/history/{id}`：查询单条

**`backend/app/services/analyzer/mouthpiece.py`**：
- `MouthpieceAnalyzer` 类：
  - `async generate(request, stream_callback)` 方法：
    1. 加载 mouthpiece System + User Prompt
    2. 调用 LLM 流式生成
    3. 解析结果 JSON
    4. 返回 `MouthpieceResult`

---

### Step 3.2 —— 创建嘴替生成前端页面

**本步创建**：

```
frontend/src/views/mouthpiece/
└── MouthpieceView.vue             # 嘴替生成主页面

frontend/src/api/mouthpiece.ts     # 嘴替生成 API 封装
```

**各文件职责**：

**`frontend/src/api/mouthpiece.ts`**：
- `generateStream(data, callbacks)`：SSE 连接到 `POST /mouthpiece/generate`
- `getHistory(page, pageSize)`、`getHistoryDetail(id)`

**`frontend/src/views/mouthpiece/MouthpieceView.vue`** —— 嘴替生成页面：
- 布局：左侧输入配置区（30%）+ 右侧预览区（70%）
- **左侧输入区**：
  - 场景选择下拉框（`el-select`）：
    - 🏢 职场吐槽
    - 💕 相亲分享
    - 🍜 探店测评
    - 🌈 生活趣事
  - 情绪风格选择（`el-radio-group`，按钮样式）：
    - 🎭 阴阳怪气
    - 😂 幽默自黑
    - 🌸 温柔发疯
    - 🗡️ 犀利毒舌
  - 情绪强度滑块（`el-slider`，1-10，带 emoji 标记两端：😊 温和 → 🔥 拉满）
  - 大白话经历文本域（`el-input`，type="textarea"，rows=6，placeholder="把你想说的都写下来...例如：老板让我周末加班，还不给加班费"）
  - "注入灵魂 ✨" 主按钮
- **右侧预览区**（初始空状态）：
  - 生成完成后展示**小红书风格预览卡片**：
    - 模拟小红书帖子 UI：圆角卡片、头像区（默认头像 + "InfoPulse 嘴替"）、标题区（大号加粗）、正文区（分段 + Emoji 彩色显示）、标签区（# 号前缀、蓝色可复制）
    - 卡片背景模拟小红书白色背景 + 轻微阴影
  - **操作栏**（卡片下方）：
    - 📋 一键复制全文（`el-button`，复制后 Toast "已复制，去小红书粘贴吧~"）
    - 🏷️ 仅复制标签
    - 🔄 重新生成（换个风格）
    - 📸 导出为图片（Phase 5 实现，先用 `el-button` 占位，disabled）

---

### Step 3.3 —— 验证嘴替生成端到端流程

**验证通过标准**：
- ✅ 输入场景 + 情绪 + 大白话 → 点击"注入灵魂"
- ✅ SSE 流式输出标题、正文、标签（打字机效果）
- ✅ 小红书风格预览卡片正确渲染
- ✅ 一键复制全文 / 复制标签功能正常
- ✅ 不同情绪风格生成的文风有明显差异

---

## Phase 4：吃瓜时间线（结构化提取 + 时间轴渲染）

> **目标**：第三个核心功能——输入事件名 → 爬取碎片信息 → AI 提取时间线 → 垂直时间轴展示。

---

### Step 4.1 —— 创建时间线模块

**本步创建**：

```
backend/app/services/prompts/templates/
├── timeline_system.txt            # 吃瓜时间线 System Prompt
└── timeline_user.txt              # 吃瓜时间线 User Prompt 模板

backend/app/schemas/timeline.py    # 时间线请求/响应 Schema
backend/app/api/timeline.py        # 时间线 API 路由
backend/app/services/analyzer/timeline.py  # 时间线分析器
```

**各文件职责**：

**`backend/app/services/prompts/templates/timeline_system.txt`**：
- 角色：专业事件调查记者
- 核心指令：
  - 从碎片信息中提取关键事件节点
  - 去情绪化：将所有主观描述改写为客观事实陈述
  - 按时间顺序排列
  - 每个节点必须标注信息源链接
- 输出格式 JSON 数组：`[{time, title, description, sources}]`

**`backend/app/schemas/timeline.py`**：
- `GenerateRequest`：event_name (str)、background (str, 可选)、verify_sources (bool, 默认 true)
- `TimelineNode`：time (str)、title (str)、description (str)、sources (List[str])
- `TimelineResult`：event_name (str)、summary (str)、nodes (List[TimelineNode])

**`backend/app/api/timeline.py`**：
- `POST /api/v1/timeline/generate`：SSE 流式返回
- `GET /api/v1/timeline/history`：分页查询
- `GET /api/v1/timeline/history/{id}`：查询单条

---

### Step 4.2 —— 创建吃瓜时间线前端页面

**本步创建**：

```
frontend/src/views/timeline/
└── TimelineView.vue               # 吃瓜时间线主页面

frontend/src/api/timeline.ts       # 吃瓜时间线 API 封装
```

**各文件职责**：

**`frontend/src/views/timeline/TimelineView.vue`** —— 吃瓜时间线页面：
- 布局：左侧输入区（30%）+ 右侧时间轴展示区（70%）
- **左侧输入区**：
  - 热点事件名称输入框（placeholder："例如：某某明星塌房事件"）
  - 补充背景信息文本域（可选，placeholder："（可选）补充你知道的背景信息..."）
  - 来源验证开关（`el-switch`，默认开启，旁边提示"开启后 AI 会附原始链接"）
  - "梳理时间线 🕐" 主按钮
- **右侧展示区**（初始空状态）：
  - 使用 Element Plus 的 `el-timeline` + `el-timeline-item` 组件
  - 每个 `el-timeline-item`：
    - `timestamp` 属性：时间（如 "2026年7月3日 14:30"）
    - 标题：`<h4>` 事件标题
    - 内容：`<p>` 客观事件描述（去情绪化）
    - 信息源：每个节点下方 `<a>` 链接列表，点击跳转原始新闻/帖子
    - 节点颜色：`#409EFF`（主色调）
  - 时间轴顶部：`<h2>` 事件名称 + `<p>` AI 生成的概述
- **操作栏**（时间轴下方）：
  - 📸 导出为图片（Phase 5）
  - 📋 复制为 Markdown（使用 `clipboard.ts` 工具）

---

## Phase 5：体验打磨（暗黑模式 + 响应式 + 错误处理）

> **目标**：把"能用的产品"打磨成"好用的产品"。

---

### Step 5.1 —— 暗黑模式

**涉及文件**：

| 文件 | 改动 |
|---|---|
| `frontend/src/assets/styles/variables.css` | 添加 `[data-theme="dark"]` 下的 CSS 变量覆盖（背景深色、文字浅色、卡片深色半透明） |
| `frontend/src/stores/app.ts` | 添加 `toggleTheme()` 方法，切换时修改 `<html data-theme>` 属性并持久化到 localStorage |
| `frontend/src/components/layout/AppHeader.vue` | 用户下拉菜单中添加"暗黑模式"开关 |

---

### Step 5.2 —— 移动端响应式适配

**涉及文件**：

| 文件 | 改动 |
|---|---|
| `frontend/src/components/layout/AppHeader.vue` | <768px 时导航菜单折叠为汉堡图标，点击弹出 `el-drawer` 侧边栏菜单 |
| `frontend/src/views/anti-scam/AntiScamView.vue` | <768px 时左右布局变为上下布局（输入区在上，结果区在下），宽度 100% |
| `frontend/src/views/mouthpiece/MouthpieceView.vue` | 同上 |
| `frontend/src/views/timeline/TimelineView.vue` | 同上 |
| `frontend/src/views/HomeView.vue` | 功能卡片从 3 列变为 1 列 |
| `frontend/src/assets/styles/global.css` | 添加 `@media (max-width: 768px)` 断点的全局响应式规则 |

---

### Step 5.3 —— 全局错误处理完善

**涉及文件**：

| 文件 | 改动 |
|---|---|
| `frontend/src/api/request.ts` | 完善拦截器：网络断开检测（`navigator.onLine`）、401 自动刷新 Token、500 全局 Toast + 上报错误日志 |
| `frontend/src/components/common/ErrorRetry.vue` | 实现错误图标 + 描述 + 重试按钮 |
| `frontend/src/utils/sse.ts` | 完善 SSE 断连重试（最多 3 次，间隔指数递增）、超时检测、兜底错误提示 |
| 所有 View 组件 | 使用 `<ErrorRetry>` 包裹可能出错的部分 |

---

### Step 5.4 —— 动画与动效优化

**涉及文件**：

| 文件 | 改动 |
|---|---|
| `frontend/src/assets/styles/transitions.css` | 完善各页面进入/离开动画（首页用 fade，工具页用 slide-left） |
| `frontend/src/components/layout/AppLayout.vue` | `<router-view>` 外包裹 `<transition>` |
| `frontend/src/components/common/LoadingOverlay.vue` | 添加渐入渐出动画 |
| 导航栏、卡片 | 确认所有 hover 动效（`transform: translateY(-2px)` + `box-shadow` + `transition: 0.3s`） |

---

### Step 5.5 —— 演示站模拟数据

**本步创建**：

```
frontend/src/assets/mock/
└── anti-scam-demo.json            # 避坑雷达模拟演示数据
```

**`frontend/src/assets/mock/anti-scam-demo.json`**：
- 预设一份完整的 `AntiScamResult` JSON 数据
- 包含：`risk_score: 85`、3-5 条模拟水军评论（带判定理由）、5-8 个模拟痛点标签
- 前端 `AntiScamView.vue` 中：如果是演示模式（`import.meta.env.VITE_DEMO_MODE === 'true'`），用户点击"开始排雷"后，播放 3-5 秒假进度条动画，然后直接渲染这份 mock 数据
- 结果底部显示："以上为模拟演示数据。获取完整版：[前往 GitHub]"

---

## Phase 6a：GitHub 开源（docker-compose + README + 清理）

> **目标**：项目可以一键部署，README 精美，代码仓库干净安全。

---

### Step 6a.1 —— 编写 docker-compose.yml

**本步创建**：

| 文件 | 说明 |
|---|---|
| `docker-compose.yml` | 项目根目录，一键启动 4 个服务（db、redis、backend、frontend） |

**`docker-compose.yml`** 内容结构：
- `services.db`：postgres:15-alpine，端口 5432，持久化 pgdata Volume，healthcheck
- `services.redis`：redis:7-alpine，端口 6379，healthcheck
- `services.backend`：build `./backend/Dockerfile`，端口 8000，环境变量从 `.env` 读取，`depends_on` db 和 redis（带 condition: service_healthy），资源限制 `memory: 2g`
- `services.frontend`：build `./frontend/Dockerfile.frontend`，端口 5173:80，depends_on backend
- `volumes.pgdata`：数据库持久化

**⚠️ 内存预检脚本**（`docker-compose.yml` 同级目录）：

| 文件 | 说明 |
|---|---|
| `check_memory.sh` | 启动前内存检测脚本（Linux/macOS） |
| `check_memory.ps1` | 启动前内存检测脚本（Windows PowerShell） |

**`check_memory.sh`** 逻辑：
1. 读取宿主机总内存（`free -m | awk '/^Mem:/{print $2}'` 或 sysctl）
2. 如果 < 4096MB → 终端红色加粗打印：`[WARNING] 检测到可用内存不足 4GB（当前：XXXMB）。运行 InfoPulse 可能会卡顿或崩溃。建议升级内存或开启至少 4GB Swap。继续运行？(y/n)`
3. 用户输入 n 则退出

**`check_memory.ps1`** 逻辑：
1. `(Get-CimInstance Win32_ComputerSystem).TotalPhysicalMemory / 1MB` 获取内存
2. < 4096MB 时同样逻辑，红色警告

**用户启动流程**（写在 README Quick Start 里）：
```bash
# 第一步：检查硬件
bash check_memory.sh     # Linux/macOS
# 或
.\check_memory.ps1       # Windows PowerShell

# 第二步：确认通过后启动
cp .env.example .env
docker-compose up -d
```

### Step 6a.2 —— 编写前端 Dockerfile

**本步创建**：

| 文件 | 说明 |
|---|---|
| `frontend/Dockerfile.frontend` | 前端多阶段构建：node:18-alpine 构建 + nginx:alpine 运行 |

### Step 6a.3 —— 编写 README.md

**本步创建**：

| 文件 | 说明 |
|---|---|
| `README.md` | GitHub 项目首页（英文版或中英双语） |
| `README_zh.md` | 中文版 README（可选，内容与 README.md 一致） |

**README.md 必备内容结构**：
1. 顶部：⚠️ 免责声明（来自 §17.9 的模板内容，用红色/加粗标注）
2. Logo + "穿透信息迷雾，洞察真实情绪"
3. 4 张产品截图（Home、AntiScam、Mouthpiece、Timeline）
4. 技术栈标签墙（Vue3 / FastAPI / Playwright / LangChain / PostgreSQL / Redis / Docker）
5. 核心功能简介（三大模块，每个一句话 + 一张截图）
6. 快速开始（3 步：clone → 配置 .env → docker-compose up -d）
7. 硬件要求（4 核 8GB 最低，16GB 推荐）
8. 项目结构（目录树速览）
9. FAQ（常见问题：Windows Docker 跑不通怎么办、如何安装小红书插件、演示站在哪）
10. 贡献指南 + License

### Step 6a.4 —— 代码清理与安全检查

**操作清单**：
- [ ] 确认 `.env.example` 中所有敏感值都是占位符（`sk-xxxxx`、`change-me`）
- [ ] 确认 `.gitignore` 包含 `.env`、`__pycache__/`、`node_modules/`、`pgdata/`、`crawler/plugins/*.py`
- [ ] 确认没有任何 `.py` 文件中硬编码了 API Key、Cookie、Token
- [ ] 确认 `crawler/plugins/` 目录下只有 `README.md`，没有任何 `.py` 爬虫文件
- [ ] 确认 README 中无"抓取小红书"等敏感措辞
- [ ] 添加 `LICENSE` 文件（MIT）
- [ ] `git init` → `git add .` → `git commit -m "🎉 Initial commit: InfoPulse v1.0"`

---

## Phase 6b：轻量演示站部署（Vercel + Render + Supabase）

> **目标**：免费公网演示站，嘴替 + 时间线可用，避坑雷达展示模拟数据。

---

### Step 6b.1 —— 部署后端到 Render

**操作步骤**：
1. 在 Render 创建新的 Web Service
2. 连接 GitHub 仓库，指定分支
3. Dockerfile 路径：`backend/Dockerfile.render`
4. 环境变量配置（在 Render Dashboard 中设置）：
   - `DATABASE_URL`：Supabase 提供的连接字符串
   - `REDIS_URL`：Upstash 提供的连接字符串
   - `JWT_SECRET_KEY`：生产环境随机字符串
   - `LLM_API_KEY`：你的 LLM API Key
   - `DEMO_MODE=true`
   - `CRAWLER_ENABLED=false`
5. 部署，获取 `*.onrender.com` 域名

### Step 6b.2 —— 配置 Supabase 数据库

1. 在 Supabase 创建免费项目
2. 运行 SQL 建表语句（`users`、`analysis_histories`、`saved_results`）
3. 获取数据库连接字符串，更新 Render 环境变量

### Step 6b.3 —— 配置 Upstash Redis

1. 在 Upstash 创建免费 Redis 实例
2. 获取连接字符串，更新 Render 环境变量

### Step 6b.4 —— 部署前端到 Vercel

1. 在 Vercel 导入 GitHub 仓库
2. 根目录设置为 `frontend`
3. 框架预设选择 Vite
4. 环境变量：`VITE_API_BASE_URL=https://xxx.onrender.com`（Render 后端地址）、`VITE_DEMO_MODE=true`
5. 部署，获取 `*.vercel.app` 域名

### Step 6b.5 —— 更新后端 CORS

在 Render 环境变量中添加 Vercel 前端域名，确保 CORS 中间件允许该来源。

---

## Phase 0-6 总文件清单（按创建顺序）

| Phase | 文件 | 类型 |
|---|---|---|
| 0 | `.gitignore` | 配置 |
| 0 | `backend/requirements.txt` | Python 依赖 |
| 0 | `backend/requirements.render.txt` | Python 依赖（轻量） |
| 0 | `backend/.env.example` | 配置模板 |
| 0 | `backend/Dockerfile` | Docker 镜像 |
| 0 | `backend/Dockerfile.render` | Docker 镜像（轻量） |
| 0 | `backend/test_playwright.py` | 测试脚本 |
| 1 | `backend/app/__init__.py` | Python 包 |
| 1 | `backend/app/main.py` | FastAPI 入口 |
| 1 | `backend/app/config.py` | 配置管理 |
| 1 | `backend/app/dependencies.py` | 依赖注入 |
| 1 | `backend/app/models/__init__.py` | 模型包 |
| 1 | `backend/app/models/user.py` | 用户表 |
| 1 | `backend/app/models/analysis_history.py` | 历史记录表 |
| 1 | `backend/app/models/saved_result.py` | 收藏表 |
| 1 | `backend/app/schemas/__init__.py` | Schema 包 |
| 1 | `backend/app/schemas/auth.py` | 认证 Schema |
| 1 | `backend/app/api/__init__.py` | API 包 |
| 1 | `backend/app/api/auth.py` | 认证路由 |
| 1 | `backend/app/services/__init__.py` | 服务包 |
| 1 | `backend/app/services/auth_service.py` | 认证逻辑 |
| 1 | `backend/app/core/__init__.py` | 核心包 |
| 1 | `backend/app/core/database.py` | 数据库连接 |
| 1 | `backend/app/core/security.py` | JWT 安全 |
| 1 | `backend/app/core/redis.py` | Redis 连接 |
| 1 | `backend/app/core/llm.py` | LLM 客户端 |
| 1 | `backend/app/middleware/__init__.py` | 中间件包 |
| 1 | `backend/app/middleware/cors.py` | CORS 配置 |
| 1 | `backend/tests/__init__.py` | 测试包 |
| 1 | `frontend/` (Vite 项目初始化) | 前端项目 |
| 1 | `frontend/src/assets/styles/variables.css` | CSS 变量 |
| 1 | `frontend/src/assets/styles/global.css` | 全局样式 |
| 1 | `frontend/src/assets/styles/transitions.css` | 过渡动画 |
| 1 | `frontend/src/components/layout/AppLayout.vue` | 全局布局 |
| 1 | `frontend/src/components/layout/AppHeader.vue` | 导航栏 |
| 1 | `frontend/src/components/layout/AppFooter.vue` | 底部状态栏 |
| 1 | `frontend/src/components/common/LoadingOverlay.vue` | 加载蒙层 |
| 1 | `frontend/src/components/common/SseTextStream.vue` | 流式文本 |
| 1 | `frontend/src/components/common/EmptyState.vue` | 空状态 |
| 1 | `frontend/src/components/common/ErrorRetry.vue` | 错误重试 |
| 1 | `frontend/src/components/auth/LoginCard.vue` | 登录卡片 |
| 1 | `frontend/src/views/HomeView.vue` | 首页 |
| 1 | `frontend/src/views/AuthView.vue` | 认证页 |
| 1 | `frontend/src/router/index.ts` | 路由配置 |
| 1 | `frontend/src/stores/user.ts` | 用户状态 |
| 1 | `frontend/src/stores/app.ts` | 全局状态 |
| 1 | `frontend/src/api/request.ts` | Axios 封装 |
| 1 | `frontend/src/api/auth.ts` | 认证 API |
| 1 | `frontend/src/utils/sse.ts` | SSE 工具 |
| 1 | `frontend/src/utils/clipboard.ts` | 剪贴板工具 |
| 1 | `frontend/src/App.vue` | 根组件 |
| 1 | `frontend/src/main.ts` | 入口 |
| 1 | `frontend/vite.config.ts` | Vite 配置 |
| 2 | `backend/app/services/crawler/__init__.py` | 爬虫注册表 |
| 2 | `backend/app/services/crawler/base.py` | 爬虫基类 |
| 2 | `backend/app/services/crawler/zhihu.py` | 知乎爬虫 |
| 2 | `backend/app/services/crawler/weibo.py` | 微博爬虫 |
| 2 | `backend/app/services/crawler/browser_manager.py` | 浏览器管理器 |
| 2 | `backend/app/services/crawler/stealth_patcher.py` | 指纹伪装 |
| 2 | `backend/app/services/crawler/plugins/README.md` | 插件说明 |
| 2 | `backend/app/services/cleaner.py` | 数据清洗 |
| 2 | `backend/app/services/prompts/__init__.py` | Prompt 包 |
| 2 | `backend/app/services/prompts/loader.py` | Prompt 加载器 |
| 2 | `backend/app/services/prompts/templates/anti_scam_system.txt` | 避坑雷达 System Prompt |
| 2 | `backend/app/services/prompts/templates/anti_scam_user.txt` | 避坑雷达 User Prompt |
| 2 | `backend/app/services/analyzer/__init__.py` | 分析器包 |
| 2 | `backend/app/services/analyzer/anti_scam.py` | 避坑雷达分析器 |
| 2 | `backend/app/schemas/anti_scam.py` | 避坑雷达 Schema |
| 2 | `backend/app/api/anti_scam.py` | 避坑雷达路由 |
| 2 | `frontend/src/views/anti-scam/AntiScamView.vue` | 避坑雷达页面 |
| 2 | `frontend/src/api/antiScam.ts` | 避坑雷达 API |
| 2 | `frontend/src/stores/history.ts` | 历史记录状态 |
| 3 | `backend/app/services/prompts/templates/mouthpiece_system.txt` | 嘴替 System Prompt |
| 3 | `backend/app/services/prompts/templates/mouthpiece_user.txt` | 嘴替 User Prompt |
| 3 | `backend/app/schemas/mouthpiece.py` | 嘴替 Schema |
| 3 | `backend/app/api/mouthpiece.py` | 嘴替路由 |
| 3 | `backend/app/services/analyzer/mouthpiece.py` | 嘴替分析器 |
| 3 | `frontend/src/views/mouthpiece/MouthpieceView.vue` | 嘴替页面 |
| 3 | `frontend/src/api/mouthpiece.ts` | 嘴替 API |
| 4 | `backend/app/services/prompts/templates/timeline_system.txt` | 时间线 System Prompt |
| 4 | `backend/app/services/prompts/templates/timeline_user.txt` | 时间线 User Prompt |
| 4 | `backend/app/schemas/timeline.py` | 时间线 Schema |
| 4 | `backend/app/api/timeline.py` | 时间线路由 |
| 4 | `backend/app/services/analyzer/timeline.py` | 时间线分析器 |
| 4 | `frontend/src/views/timeline/TimelineView.vue` | 时间线页面 |
| 4 | `frontend/src/api/timeline.ts` | 时间线 API |
| 5 | `frontend/src/assets/mock/anti-scam-demo.json` | 模拟数据 |
| 5 | 多个文件微调 | 暗黑模式、响应式、动画、错误处理 |
| 6a | `docker-compose.yml` | 容器编排 |
| 6a | `check_memory.sh` | 内存预检脚本（Linux/macOS） |
| 6a | `check_memory.ps1` | 内存预检脚本（Windows） |
| 6a | `frontend/Dockerfile.frontend` | 前端 Docker 镜像 |
| 6a | `README.md` | 项目说明 |
| 6a | `README_zh.md` | 中文说明 |
| 6a | `LICENSE` | MIT 许可证 |

---

**文档版本**：v1.0
**最后更新**：2026-07-05
