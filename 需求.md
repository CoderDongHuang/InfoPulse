# InfoPulse 产品需求文档

> 版本：v4.0
> 更新日期：2026-07-20
> 状态：核心功能已实现，进入联调与质量完善阶段

## 1. 产品定义

InfoPulse 是面向普通互联网用户、内容创作者和舆情观察者的 AI 舆情洞察工作台。产品不要求用户理解爬虫、提示词或模型参数，而是直接回答五类问题：

1. 这个话题现在大家在说什么？
2. 支持、质疑和中立观点各占多少？
3. 事件是怎样一步步发展到现在的？
4. 热搜为什么突然升温？
5. 我想表达观点，但怎样说得更清楚？

产品核心不是“抓取日志”，而是把公开讨论整理为可阅读、可追溯、可继续行动的结果。

## 2. 目标用户

| 用户 | 核心需求 | 产品价值 |
| --- | --- | --- |
| 吃瓜与围观用户 | 快速补齐事件背景 | 少翻页面，先看时间线和关键观点 |
| 内容创作者 | 找角度、写观点、核对风向 | 洞察与表达创作形成连续工作流 |
| 品牌与运营人员 | 观察讨论情绪和风险 | 多来源样本、代表观点、历史档案 |
| 学生与研究者 | 学习舆情事件结构 | 来源链接、事实风险、可解释结论 |

## 3. 产品范围

### 3.1 本期必须提供

- 用户注册、登录、刷新会话和退出。
- 微博、B站、百度贴吧三个默认公开数据源。
- 热点洞察、表达创作、事件脉络、热搜解读四个核心模块。
- 洞察、表达文案和时间线结果自动进入统一内容档案。
- LLM 未配置或调用失败时仍能返回本地降级结果。
- 桌面端和移动端均可完成核心操作。

### 3.2 本期明确不做

- 不采集私信、好友圈、付费内容或其他非公开数据。
- 不提供验证码破解、设备指纹伪装、批量账号或代理池对抗。
- 不默认接入小红书、淘宝、京东、抖音、快手、知乎。
- 不把 AI 摘要作为新闻事实或法律结论。
- 不实现无人审核的自动发帖。

## 4. 信息架构

| 页面 | 路由 | 是否登录 | 主要任务 |
| --- | --- | --- | --- |
| 今日工作台 | `/` | 否；登录后增强 | 今日讨论榜、趋势摘要、模块入口 |
| 登录注册 | `/auth` | 否 | 建立或恢复账号会话 |
| 热点洞察 | `/insight` | 是 | 输入话题并生成舆情报告 |
| 表达工作室 | `/mouthpiece` | 是 | 将原始想法改写为可发布文案 |
| 事件脉络 | `/timeline` | 是 | 从公开线索生成事件时间线 |
| 热搜解读 | `/hot-search` | 是 | 浏览热榜并生成背景摘要 |
| 内容档案 | `/history` | 是 | 查看与删除个人生成记录 |

`/anti-scam` 只保留兼容重定向，正式产品路由统一为 `/insight`。

## 5. 功能需求

### 5.1 今日工作台

**用户目标**：打开产品后立即知道今天有什么值得看，不需要先学习工具。

**页面内容**：

- 一条视觉主稿，展示微博当前榜首话题。
- 实时讨论榜，首页最多显示 6 条。
- 实时话题数、最高热度和分类覆盖摘要。
- 热点洞察、表达创作和事件脉络快捷入口。

**交互要求**：

- 外部热榜加载时只显示局部骨架，不允许整页变灰或阻塞。
- 未登录用户点击需鉴权功能时进入登录页，登录后返回原目标页面。
- 热榜不可用时展示来源状态和重试入口，不使用伪造数据冒充实时榜单。

### 5.2 热点洞察

**输入**：

- 话题关键词，2 至 80 个字符。
- 来源多选，默认微博、B站、百度贴吧，至少选择一个。
- 最大样本量，6 至 90 条。

**处理**：

1. 并发采集已选来源。
2. 单来源失败时继续处理其他来源。
3. 清洗 HTML、空白和重复内容。
4. 计算本地情绪基线。
5. 调用 LLM 生成概览、观点聚类、代表观点和风险提示。
6. 保存结果到用户历史。

**输出**：

- 话题概览和生成时间。
- 正向、中立、负向情绪比例。
- 来源状态与各来源样本数量。
- 关键观点、代表讨论、原始链接。
- 可信度、总样本数和事实核验风险。

**实时反馈**：SSE 至少包含 `progress`、`ping`、`result`、`error` 四类事件。前端只显示用户可理解的阶段，不展示底层爬虫日志。

### 5.3 表达工作室

**输入**：原始想法、发布场景、语气、情绪强度和篇幅。

**输出**：标题、正文、话题标签和 3 个备选版本。

**要求**：

- 保留用户事实与立场，不新增具体经历。
- 不生成针对具体个人的侮辱、威胁或隐私曝光内容。
- 支持一键复制。
- 结果自动保存到内容档案。
- 模型不可用时返回结构完整的本地改写结果。

### 5.4 事件脉络

**输入**：事件名称、来源和最大样本量。

**输出**：事件摘要、时间线节点、节点来源、原始链接、可信度和待确认问题。

**要求**：

- 合并重复线索。
- 没有明确时间时标记“时间待核验”，不得伪造日期。
- 网友猜测不能写成确定事实。
- 没有有效公开样本时明确提示更换关键词。
- 结果自动保存到内容档案。

### 5.5 热搜解读

**输入**：用户从热榜中选择一个话题。

**输出**：排名、热度信号、来源、分类、原始链接和 80 字以内背景摘要。

**要求**：

- 热榜接口设置短超时并缓存 5 分钟。
- 榜单加载完成后立即展示；AI 解释异步补齐，不阻塞榜单。
- 外部热榜不可用时返回清楚标记的本地示例数据。
- 用户可把热榜词一键带入热点洞察。

### 5.6 内容档案

- 统一展示 `insight`、`mouthpiece`、`timeline` 三类记录。
- 支持全部或按模块筛选。
- 显示标题、摘要、类型、创建日期和继续创作入口。
- 用户只能读取和删除自己的记录。
- 删除必须二次确认，成功后立即从列表移除。

### 5.7 账号与会话

- 用户名 3 至 50 字符，密码至少 6 位。
- 登录支持用户名或邮箱。
- Access Token 与 Refresh Token 分离。
- Token 存入当前标签页的 `sessionStorage`，关闭标签页后自动清除。
- 多个请求同时遇到 401 时只允许一次刷新请求。
- Refresh Token 无效时清理本地会话并回到登录页。

## 6. 数据源要求

| 来源 | 方式 | 是否需要 Cookie | 失败策略 |
| --- | --- | --- | --- |
| 微博 | `httpx` 调用移动公开接口 | 可选，建议配置 | 标记失败或空结果，继续其他来源 |
| B站 | `httpx` 调用公开搜索与评论接口 | 否 | 标记失败，继续其他来源 |
| 百度贴吧 | Playwright 渲染搜索页 | 可选，建议配置 | 45 秒超时，关闭页面，继续其他来源 |

所有 Cookie 只能进入本机 `.env`，不得写入代码、文档示例、日志、数据库或 Git。

## 7. AI 与降级要求

- 支持 OpenAI 协议兼容服务，通过 `LLM_API_BASE`、`LLM_API_KEY`、`LLM_MODEL` 配置。
- 所有提示词必须要求“只基于给定样本，不补造事实”。
- JSON 输出解析失败、网络超时、额度不足或 Key 缺失时自动降级。
- 降级结果必须保持相同字段结构，前端不需要写两套渲染逻辑。
- API 不得向用户返回内部异常堆栈、密钥或第三方响应原文。

## 8. 非功能需求

### 8.1 性能

- 普通 API 默认 30 秒客户端超时。
- 单来源采集超时：微博/B站 22 秒，贴吧 45 秒。
- SSE 每 5 秒发送一次心跳，前端总超时 70 秒。
- 热榜缓存 5 分钟。
- 贴吧 Chromium 同一时刻只执行一个任务。

### 8.2 稳定性

- Redis 不可用时后端仍可启动。
- Chromium 每 20 个任务或进程树内存超过 `BROWSER_RESTART_MB` 时重启。
- 每次贴吧任务必须关闭页面。
- 浏览器清理只能终止当前后端启动的子进程，不能扫描并误杀用户浏览器。

### 8.3 安全

- 密码使用 bcrypt 哈希，哈希计算放入线程池，避免阻塞事件循环。
- 所有业务接口必须鉴权。
- 历史记录查询和删除必须按 `user_id` 隔离。
- 请求模型必须限制字符串长度、列表长度和数字范围。
- `.env` 必须同时被 Git 和 Docker 构建上下文排除。

### 8.4 体验与视觉

- 用户界面只显示结果、阶段和可行动作，不打印爬虫控制台日志。
- 五个核心页面使用不同视觉语言，但共享导航、字体尺度和可访问状态。
- 页面不允许出现横向文档溢出；复杂榜单在移动端使用局部滚动。
- 图标按钮必须有可访问名称；加载状态使用 `aria-busy` 或 `role=status`。
- 图片必须展示真实的新闻、讨论或编辑场景，不能只使用装饰性渐变。

## 9. API 清单

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `POST` | `/api/v1/auth/register` | 注册并返回双 Token |
| `POST` | `/api/v1/auth/login` | 登录 |
| `POST` | `/api/v1/auth/refresh` | 轮换 Token |
| `GET` | `/api/v1/auth/me` | 当前用户 |
| `POST` | `/api/v1/insights/analyze` | SSE 热点洞察 |
| `POST` | `/api/v1/mouthpiece/generate` | 表达文案生成 |
| `POST` | `/api/v1/timeline/build` | 事件脉络 |
| `GET` | `/api/v1/hot-search/ranking` | 热榜 |
| `POST` | `/api/v1/hot-search/explain` | 热榜解释 |
| `GET` | `/api/v1/history` | 统一内容档案 |
| `DELETE` | `/api/v1/history/{id}` | 删除个人记录 |
| `GET` | `/api/v1/health` | 健康状态 |

## 10. 验收标准

- 后端编译通过，单元测试全部通过。
- 前端 TypeScript 检查和生产构建通过。
- 注册、登录、刷新、四个模块和档案接口完成一次真实联调。
- 桌面端检查 `/`、`/insight`、`/mouthpiece`、`/timeline`、`/hot-search`。
- 移动端检查 `/auth`、`/`、`/insight`。
- 上述视口无控制台错误、无文档级横向溢出、无内容重叠。
- 未配置 LLM 时，表达文案、洞察和时间线仍返回结构化结果。

## 11. 后续迭代

- 历史报告详情页与导出图片/PDF。
- 更细粒度的实体、传播节点和观点演化分析。
- 热榜多平台聚合，但新增来源必须先完成稳定性和合规评审。
- PostgreSQL Alembic 正式迁移流程与生产监控指标。
