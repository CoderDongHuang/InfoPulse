# InfoPulse 开发步骤与实施清单

> 版本：v4.0
> 更新日期：2026-07-20
> 说明：本文同时记录“已经完成的实现”和“下一步应如何继续”，每个阶段均列出文件、职责和验收方式。

## 0. 开发规则

1. 每完成一个阶段，都要回看 `需求.md` 和 `架构说明书.md`。
2. 新功能先定义请求/响应契约，再写 API 和页面。
3. 任何来源失败必须可降级，不能让整个工作流返回 500。
4. 前端只展示用户结果、进度和错误建议，不展示爬虫控制台日志。
5. `.env`、Cookie、API Key、数据库和日志永远不提交 Git，也不复制进 Docker 镜像。
6. 每次修改先跑局部测试，结束前再跑完整编译、构建和页面截图。

## Phase 0：工程与运行环境（已完成）

### Step 0.1：建立根目录交付入口

| 文件 | 内容 |
| --- | --- |
| `README.md` | 产品简介、功能、技术栈、本地启动、Compose、环境变量、验证和合规说明 |
| `.gitignore` | 排除 `.env`、数据库、日志、依赖目录和私有插件 |
| `docker-compose.yml` | PostgreSQL、Redis、后端、前端四服务编排 |
| `需求.md` | 当前产品范围和验收标准 |
| `架构说明书.md` | 当前代码架构、数据流和部署边界 |
| `开发步骤.md` | 逐阶段实施清单 |

验收：新开发者只阅读根 README 即可启动项目，不需要查聊天记录。

### Step 0.2：后端镜像

| 文件 | 内容 |
| --- | --- |
| `backend/Dockerfile` | Python 3.11、Playwright 依赖、Chromium、FastAPI 启动 |
| `backend/Dockerfile.render` | 无 Chromium 的轻量演示镜像，强制关闭爬虫 |
| `backend/.dockerignore` | 排除 `.env`、数据库、日志、测试缓存 |
| `backend/requirements.txt` | 完整依赖 |
| `backend/requirements.render.txt` | 演示站轻量依赖 |
| `backend/test_playwright.py` | Chromium 冒烟测试 |

验收命令：

```powershell
cd backend
python test_playwright.py
docker build -t infopulse-backend .
```

### Step 0.3：前端生产镜像

| 文件 | 内容 |
| --- | --- |
| `frontend/Dockerfile` | Node 构建，Nginx 静态运行 |
| `frontend/nginx.conf` | SPA 回退、静态缓存、`/api` 代理、SSE 禁止缓冲 |
| `frontend/.dockerignore` | 排除依赖、构建结果和环境文件 |

验收：

```powershell
docker compose up --build
```

检查 `http://127.0.0.1:5173`、`http://127.0.0.1:8000/api/v1/health` 和 `http://127.0.0.1:8000/docs`。

## Phase 1：后端基础设施（已完成）

### Step 1.1：配置中心

`backend/app/config.py`：

- 数据库、Redis、JWT、LLM、爬虫和演示模式配置。
- `BROWSER_RESTART_MB` 可按机器资源调整。
- 只保留微博和贴吧 Cookie 配置。
- `extra = "ignore"` 兼容旧 `.env` 中已废弃字段，升级不会直接崩溃。

`backend/.env.example`：只放占位值，不能出现真实 Cookie 或 Key。

### Step 1.2：数据库与模型

| 文件 | 职责 |
| --- | --- |
| `core/database.py` | 延迟创建异步 Engine 和 Session，启动建表，关闭释放连接 |
| `models/user.py` | 用户账号和关系 |
| `models/analysis_history.py` | 三类生成结果的 JSON 快照 |
| `models/__init__.py` | 确保启动时导入全部模型 |

本地开发使用：

```env
DATABASE_URL=sqlite+aiosqlite:///./infopulse.db
```

Compose 使用 PostgreSQL。后续修改生产表结构时新增 Alembic 迁移，不直接依赖 `create_all()`。

### Step 1.3：Redis

`core/redis.py` 负责连接和关闭。Redis 目前不是核心请求的强依赖，连接失败只记录警告，服务继续运行。

### Step 1.4：认证

| 文件 | 职责 |
| --- | --- |
| `core/security.py` | bcrypt、JWT 创建和验证 |
| `services/auth_service.py` | 注册、登录、用户查询和更新 |
| `schemas/auth.py` | 账号请求和响应模型 |
| `api/auth.py` | `/register`、`/login`、`/refresh`、`/me` |
| `dependencies.py` | Bearer Token 提取与当前用户依赖 |

关键实现：

- bcrypt 在 Starlette 线程池执行，避免阻塞事件循环。
- Refresh Token 必须包含 `type=refresh`。
- 刷新时重新查询用户状态并轮换双 Token。

## Phase 2：数据源与热点洞察（已完成）

### Step 2.1：统一爬虫接口

`services/crawler/base.py` 定义 `RawPost`、`RawComment` 和 `BaseCrawler`。

`services/crawler/__init__.py` 只注册：

```python
CRAWLER_REGISTRY = {
    "weibo": WeiboCrawler,
    "bilibili": BilibiliCrawler,
    "tieba": TiebaCrawler,
}
```

已删除知乎、淘宝、京东和小红书的旧默认适配器。`plugins/` 仅作为私有部署扩展目录，不进入产品默认界面。

### Step 2.2：微博适配器

`services/crawler/weibo.py`：

- 复用异步 HTTP 客户端。
- 搜索移动公开接口并清理 HTML。
- 解析帖子和热门评论。
- Cookie 缺失、返回 HTML 或限流时返回空结果，不抛出到整个工作流。
- `close()` 必须关闭客户端。

### Step 2.3：B站适配器

`services/crawler/bilibili.py`：

- 使用公开搜索接口和评论接口。
- 不伪造 Cookie。
- 分页搜索，评论最多 3 页。
- 保留视频原始 URL、作者和发布时间。

### Step 2.4：贴吧与浏览器管理

| 文件 | 职责 |
| --- | --- |
| `crawler/tieba.py` | 页面搜索、评论提取、Cookie 解析、URL 编码 |
| `crawler/browser_manager.py` | Chromium 单例、任务锁、内存与任务计数重启 |
| `crawler/stealth_patcher.py` | 贴吧渲染页的基础浏览器兼容处理 |

实现要求：

- 同一时刻只运行一个贴吧浏览器任务。
- 页面在 `finally` 中关闭。
- 页面关闭失败也必须释放任务锁。
- 内存统计包含 Chromium 子进程。
- 只能清理当前 Python 后端创建的浏览器子进程。

### Step 2.5：工作流聚合

`services/workflows.py`：

- `collect_posts()` 并发采集来源、设置单源超时、去重和截断。
- `fallback_insight()` 生成本地情绪与代表观点。
- `analyze_insight()` 在本地结果上合并 LLM 增强。
- 任何 LLM 异常回到本地结果。

### Step 2.6：SSE API

`api/insights.py`：

1. 创建内部 `asyncio.Queue`。
2. 后台 Producer 采集、分析和保存。
3. Stream 每 5 秒发送 `ping`。
4. 依次发送 `progress` 和 `result`。
5. 内部异常写服务端日志，前端只收到统一错误。

`frontend/src/utils/sse.ts`：

- 支持多行 `data`。
- 支持 JSON 错误事件。
- 心跳会重置活动时间。
- 70 秒总超时和主动取消。

### Step 2.7：热点洞察页面

`frontend/src/views/insight/InsightView.vue`：

- 深色来源轨展示微博、B站、贴吧状态。
- 主区域只展示输入、阶段和报告。
- 支持 URL 查询参数 `q` 自动带入热榜话题。
- 报告包含情绪、可信度、来源、关键观点和代表链接。
- 移动端来源轨转为顶部区域，不产生页面横向溢出。

正式路由是 `/insight`，旧 `/anti-scam` 仅重定向。

## Phase 3：表达工作室（已完成）

### Step 3.1：请求与服务

- `MouthpieceRequest` 限制正文、场景、语气、强度和篇幅。
- `generate_mouthpiece()` 优先生成本地结构，再尝试 LLM。
- `api/mouthpiece.py` 保存 `module=mouthpiece` 历史。

### Step 3.2：写作室页面

`frontend/src/views/mouthpiece/MouthpieceView.vue`：

- 暖色编辑工作室，不复用洞察仪表盘。
- 左侧控制内容和语气，右侧纸张预览。
- 支持复制正文和切换备选版本。
- 空状态、加载状态和错误状态不改变整体布局尺寸。

## Phase 4：事件脉络（已完成）

### Step 4.1：构建时间线

- `TimelineRequest` 使用同一平台约束并去重。
- `build_timeline()` 不伪造时间；时间缺失标记“时间待核验”。
- `api/timeline.py` 无样本时返回 422，成功后保存历史。

### Step 4.2：档案室页面

`frontend/src/views/timeline/TimelineView.vue`：

- 使用案卷、档案编号和纵向节点语言。
- 节点显示来源、可信度和原始链接。
- 先输入事件，再把结果作为“打开的案件档案”展示。

## Phase 5：热搜解读（已完成）

### Step 5.1：热榜服务

`fetch_hot_rankings()`：

- 请求 B站公开热门接口。
- HTTP 超时 5 秒。
- 进程内缓存 300 秒。
- 外部不可用时返回本地示例数据。

`HotItemRequest` 对标题、平台、热度、URL 和分类限制长度与范围。

### Step 5.2：监控台页面

`frontend/src/views/HotSearchView.vue`：

- 深色高密度信号监控台。
- 榜单加载使用局部信号骨架，不遮罩整页。
- 榜单返回后立即展示，首条 AI 解释异步加载。
- 可跳转完整洞察或打开原始来源。

## Phase 6：首页（已完成）

### Step 6.1：首页

`frontend/src/views/HomeView.vue`：

- 新闻编辑台主视觉和真实新闻场景图片。
- 微博实时讨论榜、趋势摘要和快捷工具。
- 热榜等待时只在榜单区域显示骨架。
- 用户无需理解技术参数即可进入四个核心任务。

## Phase 7：统一历史与会话完善（已完成）

### Step 7.1：统一历史 API

`api/history.py`：

- `GET /api/v1/history` 查询当前用户三类记录。
- 可选 `module` 参数筛选。
- 支持分页。
- `DELETE /api/v1/history/{id}` 只能删除当前用户记录。

旧 `/api/v1/insights/history` 暂保留兼容，不再由新前端调用。

### Step 7.2：内容档案页面

`frontend/src/views/HistoryView.vue`：

- 纸质情报档案风格。
- 筛选全部、热点洞察、表达文案和事件脉络。
- 显示标题、摘要、类型、日期和继续创作入口。
- 删除前二次确认。

### Step 7.3：前端会话

`stores/user.ts`：

- Access/Refresh Token 保存到 `sessionStorage`。
- 应用加载时恢复用户资料。
- 多个 401 共享同一个 `refreshInFlight` Promise。
- 刷新失败统一退出。

## Phase 8：视觉系统（已完成）

### Step 8.1：共享框架

| 文件 | 内容 |
| --- | --- |
| `assets/styles/variables.css` | 颜色、间距、阴影和容器变量 |
| `assets/styles/global.css` | 页面基础、排版、按钮和响应式规则 |
| `components/layout/AppHeader.vue` | 核心导航、用户菜单和移动端入口 |
| `components/layout/AppFooter.vue` | 日期、在线状态和版权信息 |
| `components/layout/AppLayout.vue` | 路由内容框架 |

### Step 8.2：页面差异化

- 登录：新闻图片与身份感明确的认证面板。
- 首页：编辑台。
- 洞察：调查桌。
- 表达创作：写作室。
- 脉络：档案室。
- 热搜：信号监控台。
- 历史：情报目录。

差异化不等于随意：导航高度、正文最小字号、按钮反馈、错误颜色和移动端断点仍保持一致。

## Phase 9：测试与验收（当前阶段）

### Step 9.1：后端静态与单元测试

```powershell
cd backend
python -m compileall -q app tests
python -m unittest discover -s tests -v
```

`tests/test_workflows.py` 至少覆盖：

- 平台去重和空平台拒绝。
- 表达强度校验。
- 本地洞察保留来源链接且情绪合计 100。
- 热榜请求模型拒绝负热度。

### Step 9.2：前端构建

```powershell
cd frontend
npm run build
```

构建必须通过 TypeScript 检查。大包警告属于性能优化项，不能掩盖真实编译错误。

### Step 9.3：API 联调

按顺序验证：

1. `/health`
2. 注册并获取双 Token
3. `/auth/me`
4. Refresh Token 轮换
5. 表达文案的本地或模型结果
6. 热榜和解释
7. 统一历史读取与删除
8. 洞察 SSE 事件完整闭合

### Step 9.4：视觉回归

桌面端 1440×1000：

- `/`
- `/insight`
- `/mouthpiece`
- `/timeline`
- `/hot-search`
- `/history`

移动端 390×844：

- `/auth`
- `/`
- `/insight`
- `/history`

每页检查：控制台错误、请求失败、横向文档溢出、文字遮挡、按钮截断、图片空白和加载遮罩。

## Phase 10：下一轮建议

### P0：发布前必须做

- 增加 Alembic 初始迁移并验证 PostgreSQL 升级流程。
- 增加 FastAPI 真实接口集成测试，不只测试纯函数。
- 为第三方请求增加结构化日志和基础指标，但日志不得含 Cookie。
- 将生产 CORS 域名改为环境配置。

### P1：体验增强

- 内容档案详情页。
- 洞察和时间线导出图片/PDF。
- 热榜来源扩展为多平台聚合，并清楚显示榜单时间。

### P2：工程优化

- Element Plus 按需加载，降低首包体积。
- 为热榜缓存接入 Redis，使多进程实例共享缓存。
- 对洞察任务引入持久化队列和任务取消。
- 增加 Playwright 浏览器进程指标和失败率告警。

## 最终完成定义

一个阶段只有同时满足以下条件才算完成：

- 代码行为符合 `需求.md`。
- 模块边界符合 `架构说明书.md`。
- 相关测试通过。
- 页面在目标视口无明显视觉问题。
- 文档已同步，不保留旧平台、旧路由或旧字段描述。
- 没有把本机凭证、数据库、日志或临时截图误提交到 Git。
