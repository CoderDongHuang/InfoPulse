# InfoPulse AI Intelligence Platform 重构方案

## 1. 产品定位

InfoPulse 从“面向普通用户的泛舆情工具”调整为“面向科技从业者、研究人员和产品团队的 AI 行业情报平台”。

产品每天自动采集公开科技信息，并结合用户知识库，将零散内容聚合成事件，让用户继续发现、搜索、追问、订阅、自动执行任务和生成报告。舆情分析是平台能力之一，而不是产品的全部。核心价值不是展示更多图表，而是回答四个问题：

1. 今天发生了什么？
2. 为什么值得关注？
3. 我接下来应该看什么或做什么？
4. 哪些工作可以交给 AI 持续完成？

## 2. 重构原则

- 先做真实数据闭环，不创建没有数据支撑的菜单和占位页面。
- 事件是核心领域对象，原始内容、实体、分析、收藏和报告都围绕事件组织。
- AI 结论必须能追溯到原始来源，并明确区分事实、推断和预测。
- 第一版聚焦 AI、开发者工具和科技行业，不承诺“全网舆情”。
- 优先使用官方 API、RSS 和稳定公开接口，微博、抖音、小红书不作为首期核心依赖。
- AI 是信息阅读和操作入口，图表只承担验证与比较作用。
- 主动推荐优先于被动查询：系统每天将海量内容压缩成少量值得关注的事项。
- 收藏解决“以后再看”，订阅解决“持续关注”，任务解决“自动执行”，通知解决“及时送达”，四者不能混为一个模块。
- 公共情报与用户私有知识必须在权限、引用和存储层面明确区分。

## 3. 完整功能范围

下列功能来自产品对话，是完整产品范围，不因分期开发而删除。分期只决定交付顺序。

### 3.1 登录、注册与首次引导

- 用户名/邮箱注册、密码登录、退出、刷新令牌、记住登录。
- 邮箱验证、忘记密码和重置密码。
- GitHub、Google 登录作为第三方身份能力。
- 首次登录选择关注行业、关键词、企业、人物、产品和数据源。
- 个人资料、头像、通知偏好、主题、语言、API Key、AI 模型和数据导出。

### 3.2 首页 Dashboard

- 今日新增内容、事件、风险事件、预警次数和总采集量。
- 今日热点 TOP10、最新事件和最新风险事件。
- 舆情/情报热度趋势、情感趋势、平台占比和来源健康状态。
- AI 今日结论、重点关注原因和可直接点击的追问建议。
- 新闻、微博、视频、论坛、GitHub、Reddit 等来源统计。

### 3.3 数据中心与全局搜索

- 搜索关键词、人物、企业、组织、产品、项目、地点和事件。
- 时间范围、平台、地区、语言、情感、热度、来源、内容类型、原创/转载、官方/非官方筛选。
- 浏览新闻、帖子、评论、视频、公众号、论坛、论文、Release 和 Issue。
- 分页、排序、保存搜索、搜索历史和热门搜索。
- 内容详情展示标题、正文、媒体、作者、时间、来源和互动量。
- AI 自动生成摘要、标签、情感、风险、实体和所属事件。

### 3.4 事件中心

- 热点、最新、风险、持续升温、已回应和已结束事件列表。
- 事件详情展示简介、阶段、涉及平台、热度曲线、来源、评论和官方回应。
- 一句话、三段、详细、领导版和日报版总结。
- 自动时间线、传播阶段、关键转折点和待核验事项。
- 相似事件、关联人物、企业、产品、政策和历史事件。
- 合并重复事件、拆分错误事件、人工修正标题与状态。

### 3.5 AI 分析中心

- AI 摘要、情感分析、观点聚类、争议点和原因分析。
- 传播路径、关键节点、意见领袖和影响力贡献分析。
- 行业、企业、品牌、产品和地域影响分析。
- 风险等级、风险原因、未来 12 小时/24 小时/7 天趋势预测。
- 企业回应、公告、客服话术、声明和行动建议。
- AI 事件复盘、舆论观点地图、关联网络和自然语言问数。
- 所有结论必须携带内容引用、数据范围、模型和生成时间。

### 3.6 评论分析

- 评论列表、分页、平台和时间筛选。
- 积极、中立、消极以及开心、支持、愤怒、悲伤、担忧等细分情绪。
- 高赞评论、热门观点、观点阵营、代表评论和观点占比。
- 评论关键词、热词趋势、异常增长和水军/重复文本提示。
- 仅对数据源合法、稳定提供的公开评论启用。

### 3.7 知识分析

- 人物、企业、组织、产品、项目、地点、行业、政策实体识别。
- 实体别名归一和人工合并。
- 实体关系图谱、事件关联网络和点击节点详情。
- 关联事件、相似事件、关联政策和标签体系。
- 实体详情展示热度、情感、风险、相关新闻和历史事件。

### 3.8 数据统计

- 平台、地域、来源、作者、发布时间和内容类型分析。
- 情感、热度、风险和事件数量趋势。
- Top100 热词、上升最快热词和词频趋势。
- 折线图、柱状图、分布图、地图、桑基图、词云和知识图谱。
- 图表筛选结果可保存、导出并交给 AI 继续解释。

### 3.9 预警中心

- 关键词、热度、负面、情感、媒体、官方来源预警。
- 传播速度、评论数量、风险分数和 AI 判断预警。
- 预警规则的阈值、数据源、时间窗口、静默期和启停设置。
- 站内、邮件、Webhook 等通知渠道。
- 触发记录、处理状态、负责人、备注和误报反馈。

### 3.10 AI 分析师与 AI 工作区

- 基于平台数据连续对话和会话历史。
- 支持行业概览、事件分析、企业比较、趋势预测和报告生成。
- 回答中生成表格、图表、事件清单和可执行建议。
- 支持固定事件/实体作为上下文，并查看每条回答的引用。
- 支持“近30天负面最多的品牌”等自然语言 BI。

### 3.11 报告中心

- 日报、周报、月报、专项、事件、行业、领导简报和风险报告。
- 报告模板、目录、引用、图表、编辑、复制和版本记录。
- 导出 Word、PDF、Markdown 和 HTML。
- 手动生成、定时生成和从事件/收藏/搜索结果生成。

### 3.12 收藏与订阅

- 收藏事件、内容、关键词、企业、人物、产品、行业和报告。
- 文件夹、标签、备注、批量移动和取消收藏。
- 订阅关键词、行业、企业、人物、产品和搜索条件。
- 每日/每周推送、即时提醒、暂停、恢复和推送历史。
- 最近浏览和个人工作台汇总。

### 3.13 数据源管理

- 查看所有数据源的类型、授权方式、启用状态和最近同步时间。
- 启停数据源、测试连接、手动同步、配置 Token 和采集频率。
- 新增 RSS/Atom 地址并预览解析结果。
- 展示成功率、限流、错误、延迟和最近抓取量。
- 数据源合规说明、字段能力和评论支持能力必须可见。

### 3.14 个人工作台与系统设置

- 我的收藏、报告、预警、订阅、最近浏览和 AI 历史对话。
- 用户资料、安全设置、登录设备和账户注销。
- AI 模型、API Key、通知、主题、语言和导出记录。
- 管理端后续支持用户、任务、数据源、模型费用和审计日志。

### 3.15 Workspace 工作台

Workspace 是登录后的默认首页，回答“我今天应该做什么”，与展示全局数据态势的 Dashboard 分开。

- 今日关注：用户订阅主题的新增量、热度变化、风险和未读内容。
- 我的收藏：最近收藏的事件、实体、关键词、内容和报告。
- 最近报告：最近生成、待编辑、已完成和定时生成的报告。
- 最近浏览：最近访问的事件、搜索、实体、报告和 AI 会话。
- 待办与任务：今日即将执行、失败、等待确认和已完成的 AI 任务。
- AI 建议：今天最值得阅读的内容、需要关注的变化及推荐理由。
- 一键继续：继续上一次分析、打开未完成报告或追问最近事件。
- 用户可调整组件顺序、折叠模块和设置默认行业。

### 3.16 Discover 发现

- 今日 AI、Java、机器人、开源、融资、产品发布、研究论文等频道。
- GitHub Trending、热门论文、开发者讨论、产品发布和官方博客更新。
- AI 自动形成“为什么值得看”和“与我有什么关系”的摘要。
- 支持关注频道、切换时间范围、地区和语言。
- 支持“不感兴趣”、减少类似推荐和推荐原因解释。
- 从发现内容可以创建事件、收藏、订阅或交给 AI Agent 深入分析。

### 3.17 AI 任务中心

- 创建一次性、定时和事件触发型 AI 任务。
- 支持每天 9 点生成日报、每周总结、定时分析企业和监测关键词。
- Cron/可视化频率、时区、输入范围、使用模型、输出格式和通知方式配置。
- 任务列表展示下次执行、最近结果、成功率、耗时、Token/费用和状态。
- 执行历史、日志、失败原因、重试、暂停、恢复和立即运行。
- 高风险外部动作必须进入等待确认状态，AI 不得自行发送或发布。
- 任务输出可以生成报告、通知、数据集或新的分析建议。

### 3.18 知识库（RAG）

- 上传 PDF、Word、Markdown、TXT，并导入公开网页。
- 建立个人或团队知识库，支持文件夹、标签和权限。
- 文件解析、分段、向量化、索引状态、失败重试和删除。
- 文档预览、来源位置、页码/段落引用和版本更新。
- AI Agent 可组合公共行业情报和用户授权的私有知识回答。
- 支持限定知识库、仅私有资料、仅公开数据或混合检索模式。
- 回答明确标识“公共来源”和“内部知识”，禁止跨用户泄露。
- 支持知识库检索测试、召回结果检查和过期内容提示。

### 3.19 通知中心

- 汇总预警触发、订阅更新、任务结果、报告完成、同步异常和系统消息。
- 未读/已读、类型筛选、批量标记、归档和跳转到关联对象。
- 每条通知显示发生时间、原因、数据范围和建议动作。
- 支持站内、邮件和 Webhook 渠道及免打扰时间。
- 同类高频通知自动合并，避免重复轰炸。
- 通知偏好按类型和订阅单独配置。

### 3.20 AI 每日分析建议

这是平台核心产品能力，不只是 Dashboard 上的一段模型总结。

- 每天从用户订阅、行业频道、历史浏览和全局事件中筛出少量重点事项。
- 每项建议包含重要程度、发生了什么、影响、推荐行动、置信度和引用。
- 解释为什么推荐给当前用户，并允许反馈“有用/无用/不感兴趣”。
- 建议可转为收藏、订阅、任务、报告或 AI Agent 会话。
- 同一事实不会因多个来源重复推荐，重大更新会标记为事件进展。
- 没有足够证据时输出“需要继续观察”，不强行给出行动建议。

### 3.21 传播路径分析

- 识别首发来源、跨平台转载、关键传播节点、媒体/KOL 和官方回应。
- 展示平台间的传播顺序、时间差、内容变体和贡献度。
- 支持时间轴、传播网络和桑基图三种视图。
- 节点可以回溯原始 URL、发布时间、作者和互动指标。
- 区分可验证传播关系与基于时间/文本相似度推断的关系。
- 对 Twitter/X、Reddit、新闻、知乎、微博等跨平台路径，仅在来源数据和授权充分时生成。
- 给出传播阶段、加速点、转折点和影响力分析。

### 3.22 独立系统设置

- 模型供应商：OpenAI、DeepSeek、Anthropic Claude、Google Gemini 和兼容 OpenAI 协议的自定义模型。
- 为对话、摘要、聚类、向量化和长报告分别选择模型。
- API Key 加密保存、连接测试、额度提示和模型降级顺序。
- 通知渠道、语言、时区、日期格式、主题和数据保留期限。
- 隐私、知识库权限、导出、删除账户和登录设备管理。
- 系统管理员配置默认数据源、模型、任务并发、费用上限和审计日志。

## 4. 首期可运行闭环

### 4.1 今日情报

- 今日采集量、形成事件数、重点事件数和风险事件数。
- 今日重点事件流，每个事件展示 AI 摘要、热度、变化趋势、来源覆盖和可信度。
- 来源分布与近七日事件趋势。
- 固定 AI 分析入口，可基于当前事件继续提问。

### 4.2 情报搜索

- 搜索事件、公司、人物、产品、项目和关键词。
- 按时间、来源、内容类型、情感和事件热度筛选。
- 结果展示标题、摘要、来源、发布时间、所属事件、标签和原始链接。
- 搜索结果可加入已有事件或创建新事件。

### 4.3 事件中心

- 事件列表支持按热度、时间、风险和状态排序。
- 事件详情包含概览、时间线、来源、观点和 AI 分析。
- AI 输出一句话摘要、完整摘要、关键转折点、争议点、影响和待核验事项。
- 同一事件中的相似内容自动去重并保留来源引用。

### 4.4 AI 分析师

- 支持围绕事件或搜索结果连续提问。
- 回答引用平台内的原始内容，不允许无来源结论伪装为事实。
- 支持“比较两个事件”“生成简报”“解释热度变化”等预置任务。
- 保存会话和上下文，后续可继续分析。

### 4.5 收藏与简报

- 收藏事件、实体、关键词和原始内容。
- 将一个或多个事件生成 Markdown 简报。
- 首期先支持手动生成，定时订阅和邮件推送在自动化阶段交付。

## 5. 分期说明

完整功能均保留在产品范围内，但按依赖关系分批交付，不建立无法工作的空壳：

- 预警规则与通知。
- 日报、周报、专项报告以及 Word/PDF 导出。
- 实体关系图谱和相似事件网络。
- 自然语言问数和动态图表。
- 自定义 RSS、数据源开关和采集频率。
- 邮件订阅、Webhook 和团队协作。

评论分析仅在数据源能够稳定提供评论时启用，不作为所有事件的强制模块。

## 6. 信息架构

```text
InfoPulse
├── 我的工作台      /workspace
├── 今日情报        /dashboard
├── 发现            /discover
├── 情报搜索        /search
├── 事件中心        /events
│   └── 事件详情    /events/:id
├── AI 分析         /analysis
├── 数据分析        /analytics
├── AI Agent        /agent
├── 简报            /reports
├── 收藏与订阅      /collections
├── 任务中心        /tasks
├── 通知中心        /notifications
├── 知识库          /knowledge
├── 数据源          /sources
└── 设置
    ├── AI 模型     /settings/models
    └── 个人资料    /settings/profile
```

导航分组：

```text
开始工作：Workspace、Dashboard、Discover
研究分析：搜索、事件、AI 分析、数据分析
AI 工作：AI Agent、任务、报告、知识库
持续关注：收藏与订阅、通知
平台管理：数据源、系统设置、个人中心
```

Workspace 是默认登录页；Dashboard 展示全局数据态势；Discover 主动提供新内容。三者职责不同，不能合并成三个近似首页。

现有路由迁移：

| 现有功能 | 处理方式 |
| --- | --- |
| `/insight` 热点洞察 | 合并到事件详情的“AI 分析” |
| `/timeline` 事件脉络 | 合并到事件详情的“时间线” |
| `/hot-search` 实时热搜 | 被今日情报和来源列表取代 |
| `/mouthpiece` 表达工作室 | 不属于核心定位，移入简报编辑器或下线 |
| `/history` 内容档案 | 分拆到 AI 会话历史和简报历史 |
| `/watchlist` 关注话题 | 迁移到收藏中心 |

## 7. 页面设计

### 7.1 全局布局

- 220px 左侧导航，展示一级模块；设置独立放在底部。
- 顶部工具栏提供全局搜索、采集状态、AI 分析入口和用户菜单。
- 主内容区域采用 1200-1440px 响应式宽度，不做大屏式满页图表墙。
- 颜色以深灰黑、白和中性灰为主，蓝色用于主要操作，绿色/琥珀色/红色只表达状态。
- 卡片圆角控制在 6-8px，减少装饰性阴影和无意义动画。
- 暗色模式作为主题选项，不让整个产品默认陷入单一深蓝色。
- 设计参考 ChatGPT 的对话、Perplexity 的检索引用、Linear 的后台布局、Notion 的文档、Cursor 的 AI 工作区和 Arc 的内容组织；只借鉴交互原则，不照搬视觉。
- AI Agent 通过全局快捷入口随处可达，并能自动携带当前事件、搜索或报告上下文。

### 7.2 Workspace

- 顶部显示日期、问候、今日待办数量和全局 AI 输入框。
- 主列依次呈现今日关注、AI 建议、未完成任务和最近报告。
- 辅助列呈现收藏、最近浏览、订阅状态和数据源异常。
- 每块内容都提供直接动作，不使用只有数字而不可操作的统计卡。
- 移动端按优先级改为单列：AI 建议、今日关注、任务、报告、收藏。

### 7.3 今日情报

- 第一屏是日期问候、采集状态和一句 AI 今日结论。
- 左侧为重点事件流，右侧为紧凑的 AI 快问面板。
- 统计指标放在事件流下方，趋势图仅保留一张可切换指标的折线图。
- 用户应能在首屏完成查看事件、收藏和追问。

### 7.4 Discover

- 顶部使用频道标签和时间范围，而不是大型营销式标题。
- 内容按“今日重点、快速上升、官方发布、开源项目、研究论文、融资产品”分区。
- 每条内容显示 AI 摘要、来源组合、热度变化和推荐原因。
- 右侧提供订阅频道和调整推荐偏好的入口。

### 7.5 搜索

- 搜索框是首要控件，默认支持自然语言和关键词。
- 桌面端左侧筛选，右侧结果列表；移动端筛选进入抽屉。
- 结果使用紧凑列表，不为每条内容创建大型卡片。
- 结果详情以侧边面板打开，避免频繁丢失筛选上下文。

### 7.6 事件详情

- 标题区显示事件状态、时间、热度、风险和来源数。
- AI 摘要直接出现在标题区下方，并附引用数量和生成时间。
- 使用“概览、时间线、观点、分析、来源”标签页。
- 右侧上下文栏提供收藏、生成简报和继续追问。
- 增加“传播路径”标签页，证据不足时显示缺失的数据源而不是生成虚假路径。

### 7.7 AI Agent

- 左侧为会话和已固定事件，右侧为对话区。
- 每个回答包含引用、所用数据范围和可继续执行的建议操作。
- 表格和图表以内嵌结果块出现，不单独跳转到 BI 页面。
- 首页提供“总结今日 AI 行业、生成日报、分析企业、比较事件、预测趋势”等真实任务模板。
- AI 可以调用搜索、事件、知识库、报告和任务工具，但每次调用都显示过程和数据范围。

### 7.8 任务、订阅与通知

- 订阅详情展示关注对象、频率、最近更新和由此创建的任务。
- 任务详情展示配置、运行记录和输出；通知展示用户需要处理的结果。
- 三者通过 ID 关联，但保持独立页面，避免用户无法判断“关注了什么、系统会做什么、发生了什么”。

### 7.9 知识库

- 左侧为知识库和文件树，中间为文档列表/预览，右侧为解析状态和引用测试。
- 上传区域是明确工具区，不把整个页面做成上传卡片。
- AI 问答可以从选中的知识库直接发起，并默认保留引用。

### 7.10 数据源中心

- 使用紧凑表格展示来源、类型、状态、同步频率、最后成功时间、数据量和错误。
- 新增 RSS 使用对话框完成 URL 验证、Feed 预览和保存。
- 数据源详情包含字段能力、同步日志、限流和连接测试。

### 7.11 登录注册

- 左侧使用真实科技新闻工作场景图片，并展示产品名和一句定位。
- 右侧是克制的登录表单，支持邮箱/用户名和密码。
- 注册与登录共用一个框架，错误在字段附近显示。
- 忘记密码、邮箱验证和第三方登录只有在后端能力完成时才显示。

## 8. 三方数据源与真实接入方式

### 8.1 已在本机验证可连通

2026-07-28 使用真实网络请求验证以下端点，不使用模拟数据：

| 数据源 | 真实端点 | 接入方式 | 验证结果与限制 |
| --- | --- | --- | --- |
| Hacker News | `hacker-news.firebaseio.com/v0` | Firebase 官方 API | 成功返回 500 个 Top Story ID；免费无密钥 |
| GitHub | `api.github.com` | REST/GraphQL 官方 API | 成功；匿名限额 60 次/小时，生产环境需要 Token |
| DEV Community | `dev.to/api/articles` | 官方 REST API | 成功返回真实文章；免费无密钥 |
| arXiv | `export.arxiv.org/api/query` | 官方 Atom API | HTTP 200，返回 `application/atom+xml` |
| V2EX | `v2ex.com/api/topics/hot.json` | 公开 API | 成功返回实时热门主题；需控制频率 |
| OpenAI News | `openai.com/news/rss.xml` | 官方 RSS | HTTP 200，返回真实 XML Feed |
| Google News | `news.google.com/rss/search` | RSS | HTTP 200；作为聚合补充，必须保留原媒体链接 |

### 8.2 可接入但需要凭证或额外条件

| 数据源 | 接入方式 | 条件 | 提供能力 |
| --- | --- | --- | --- |
| Reddit | OAuth API | 注册应用、Client ID/Secret、遵守限额 | 帖子、评论、社区热度 |
| Product Hunt | GraphQL API | Developer Token | 产品、话题、投票和评论 |
| GitHub 高额度 | REST/GraphQL | Personal/GitHub App Token | Release、Issue、仓库、用户和搜索 |
| YouTube | Data API v3 | Google API Key 和配额 | 视频、频道、评论 |
| NewsAPI 等新闻服务 | 商业 API | 免费额度通常限制生产使用 | 新闻搜索和媒体元数据 |

### 8.3 RSS/Atom 扩展源

- OpenAI、Google、Microsoft、GitHub、Cloudflare 等官方博客 Feed。
- InfoQ、少数派、开源中国、Linux 中国等明确提供 RSS 的科技媒体。
- arXiv 分类或关键词 Atom 查询。
- 用户自定义 RSS，入库前必须经过 URL 校验、超时、大小限制和内容清洗。

每条 RSS 内容必须保存 Feed 地址、原文地址、发布日期、作者和抓取时间。媒体没有明确 RSS 时不伪装成“官方 RSS”。

### 8.4 非官方接口和网页采集

| 数据源 | 当前方式 | 真实情况 | 产品策略 |
| --- | --- | --- | --- |
| 微博搜索 | `m.weibo.cn/api/container/getIndex` | 非正式公开接口，常需 Cookie，可能返回 432/验证页 | 可选实验源，必须显示健康状态 |
| 微博热搜 | `weibo.com/ajax/statuses/hot_band` | 非正式公开接口，可能限流 | 可选榜单，不作为首页唯一数据 |
| B站 | `api.bilibili.com/x/web-interface`、`x/v2/reply` | 网页公开接口，非开放平台承诺 | 可选中文视频源，缓存并限频 |
| 百度贴吧 | Playwright 解析公开页面 | 页面结构易变、成本高 | 可选实验源，失败不得阻塞任务 |
| 知乎、抖音、小红书、公众号 | 网页/封闭生态 | 反爬和授权限制明显 | 没有正式授权前不承诺稳定覆盖 |

### 8.5 统一字段能力

所有接入器必须输出统一的 `ContentItem`：来源 ID、外部 ID、URL、标题、正文、作者、发布时间、抓取时间、类型、语言、互动量、原始数据和内容哈希。来源不提供的字段保存为空，不能用生成值冒充真实数据。

### 8.6 真实数据验收

- 每个来源至少有契约测试、真实连通性测试和保存样例。
- Dashboard 上的数字必须来自数据库聚合，不允许前端硬编码。
- 任何来源不可用时展示错误和最后成功时间，不注入演示数据。
- 原始内容可以通过 URL 回溯，内容哈希保证重复同步不重复入库。
- API、RSS、网页采集在界面中明确区分，用户能看到授权和稳定性。

Reddit 需要应用授权和额度管理，作为可选来源。Google News RSS 的稳定性和使用条款需要单独验证，不作为唯一新闻入口。

## 9. 核心数据模型

```text
Source
  id, name, kind, base_url, enabled, health_status, last_sync_at

ContentItem
  id, source_id, external_id, url, title, body, author,
  content_type, published_at, fetched_at, language,
  engagement, raw_payload, content_hash

Event
  id, title, slug, summary, status, category,
  heat_score, risk_score, confidence, started_at,
  last_activity_at, created_at, updated_at

EventContent
  event_id, content_id, relevance_score, is_primary

Entity
  id, name, entity_type, description

EventEntity
  event_id, entity_id, role, confidence

Analysis
  id, event_id, analysis_type, result, citations,
  model, prompt_version, created_at

CollectionItem
  id, user_id, target_type, target_id, note, created_at

Conversation / Message
  id, user_id, event_id, title, created_at
  conversation_id, role, content, citations, created_at

Subscription
  id, user_id, target_type, target_id, query,
  schedule, timezone, channels, enabled, last_run_at, next_run_at

AgentTask / TaskRun
  id, user_id, name, task_type, schedule, input_config,
  output_config, model_config, approval_policy, enabled, next_run_at
  task_id, status, started_at, finished_at, result, error, usage

Notification
  id, user_id, notification_type, title, body,
  target_type, target_id, is_read, group_key, created_at

KnowledgeBase / KnowledgeDocument / KnowledgeChunk
  id, owner_id, name, visibility, embedding_model, created_at
  id, knowledge_base_id, filename, mime_type, source_url,
  status, content_hash, version, created_at
  id, document_id, chunk_index, content, locator, embedding

Recommendation / RecommendationFeedback
  id, user_id, event_id, score, reason, impact,
  suggested_action, citations, generated_at, expires_at
  recommendation_id, user_id, feedback_type, created_at

PropagationNode / PropagationEdge
  id, event_id, content_id, platform, occurred_at, node_type
  id, event_id, from_node_id, to_node_id, relation_type,
  confidence, evidence
```

`ContentItem.url`、`(source_id, external_id)` 和 `content_hash` 建立唯一约束，避免重复采集。事件与内容必须通过关联表连接，不能把事件简单存成一次 AI 返回的 JSON。

## 10. 后端边界

```text
app/
├── api/
│   ├── dashboard.py
│   ├── search.py
│   ├── events.py
│   ├── analyst.py
│   ├── collections.py
│   ├── subscriptions.py
│   ├── tasks.py
│   ├── notifications.py
│   ├── knowledge.py
│   ├── recommendations.py
│   └── sources.py
├── domain/
│   ├── content/
│   ├── events/
│   └── analysis/
├── collectors/
│   ├── github.py
│   ├── hacker_news.py
│   ├── devto.py
│   ├── arxiv.py
│   └── rss.py
├── services/
│   ├── ingestion.py
│   ├── event_clustering.py
│   ├── event_scoring.py
│   ├── search.py
│   ├── analysis.py
│   ├── recommendations.py
│   ├── propagation.py
│   └── retrieval.py
└── jobs/
    ├── sync_sources.py
    ├── refresh_events.py
    ├── run_agent_tasks.py
    └── deliver_notifications.py
```

采集器只负责获取和标准化；事件聚合、评分和 AI 分析不能写进采集器。后台任务第一版可使用 FastAPI 生命周期内的轻量调度器，部署后再迁移到独立 Worker。

## 11. API 草案

```text
GET    /api/v1/dashboard
GET    /api/v1/workspace
GET    /api/v1/discover
GET    /api/v1/search?q=&source=&type=&from=&to=&page=
GET    /api/v1/events
GET    /api/v1/events/{id}
GET    /api/v1/events/{id}/timeline
GET    /api/v1/events/{id}/sources
POST   /api/v1/events/{id}/analyze
GET    /api/v1/sources
PATCH  /api/v1/sources/{id}
POST   /api/v1/sources/sync
GET    /api/v1/collections
POST   /api/v1/collections
DELETE /api/v1/collections/{id}
GET    /api/v1/subscriptions
POST   /api/v1/subscriptions
PATCH  /api/v1/subscriptions/{id}
GET    /api/v1/tasks
POST   /api/v1/tasks
POST   /api/v1/tasks/{id}/run
GET    /api/v1/tasks/{id}/runs
GET    /api/v1/notifications
PATCH  /api/v1/notifications/{id}
POST   /api/v1/knowledge-bases
POST   /api/v1/knowledge-bases/{id}/documents
GET    /api/v1/knowledge-bases/{id}/documents
DELETE /api/v1/knowledge-bases/{id}/documents/{document_id}
GET    /api/v1/recommendations/today
POST   /api/v1/recommendations/{id}/feedback
GET    /api/v1/events/{id}/propagation
POST   /api/v1/conversations
POST   /api/v1/conversations/{id}/messages
GET    /api/v1/reports
POST   /api/v1/reports
```

列表接口统一使用 `items`、`total`、`page`、`page_size`。所有 AI 返回统一包含 `citations`、`model`、`generated_at` 和 `confidence`。

## 12. 迁移计划

### 阶段一：情报数据闭环

1. 新建 Source、ContentItem、Event 和 EventContent 模型及迁移。
2. 接入 Hacker News、GitHub Release、DEV.to、arXiv 和 RSS。
3. 实现标准化、去重、事件聚合和热度评分。
4. 实现 Dashboard、搜索、事件列表和事件详情 API。
5. 重写前端导航、今日情报、搜索和事件详情。

验收标准：本地一次同步可以产生真实内容和事件；用户能从首页进入事件、核对来源、搜索并收藏。

### 阶段二：AI 分析闭环

1. 为事件生成带引用的摘要、观点、风险和时间线。
2. 建立会话与消息模型，实现基于已采集内容的连续追问。
3. 将现有洞察和时间线提示词迁移到新的 Analysis 服务。
4. 支持从事件生成 Markdown 简报。

验收标准：AI 回答包含可点击引用；禁用模型时仍能浏览和搜索全部真实数据。

### 阶段三：自动化

1. 数据源管理和定时采集。
2. 收藏关键词转订阅规则。
3. 预警、日报、邮件/Webhook 通知。
4. 实体网络、相似事件和自然语言问数。

### 阶段四：AI 工作操作系统

1. Workspace 个性化聚合、最近浏览和跨模块继续工作。
2. Agent 工具调用、任务调度、运行历史、费用和审批机制。
3. 知识库文档解析、向量检索、权限隔离和混合引用。
4. Discover 推荐、用户反馈和每日 AI 分析建议。
5. 传播路径证据图、跨平台关系置信度和影响力贡献。

验收标准：订阅能按计划创建任务，任务执行后产生报告或通知；AI 回答能同时引用公共情报和用户授权知识，并清晰区分来源。

## 13. 现有代码处置

可复用：

- 用户、JWT、异步数据库和请求鉴权。
- LLM 兼容层和 JSON 输出解析。
- SSE 传输、采集器抽象、清洗和去重思路。
- 时间线、来源引用和历史记录中的部分展示组件。

需要替换：

- 将 `AnalysisHistory.output_result` 作为主要业务存储的方式。
- 围绕一次请求临时抓取三个社交平台的工作流。
- 各页面互不一致的视觉主题和重复 scoped CSS。
- 全量注册 Element Plus 图标导致的前端主包体积问题。

需要下线或降级：

- 将微博接口作为首页可靠数据源。
- 与行业情报定位无关的“表达工作室”独立一级模块。
- 没有稳定数据支撑的评论、地域和传播路径承诺。

## 14. 完成定义

重构完成不是菜单数量达到 11 个，而是满足以下条件：

- 数据来自稳定、可说明的数据源，并可重复同步。
- 同一内容不会重复入库，事件能随新内容持续更新。
- 首页、搜索、事件详情和 AI 分析使用同一组领域数据。
- 每条 AI 事实结论都能回到原始内容核验。
- 模型或某个来源不可用时，核心浏览与搜索仍可工作。
- 桌面和移动端均不存在导航遮挡、文本溢出和关键操作不可达。
- Workspace 能解释今日优先事项并让用户直接继续工作，而不是重复 Dashboard 指标。
- 订阅、任务、通知形成可追踪链路，任何自动执行都有运行日志和失败状态。
- 知识库回答具备文件、页码或段落引用，并通过用户权限隔离。
- 每日建议能够说明推荐原因、影响和行动建议，并接受用户反馈。
- 传播路径中的推断关系明确展示置信度和证据，不伪装为已验证转载关系。
