<script setup lang="ts">
import{nextTick,onBeforeUnmount,onMounted,ref}from'vue';import{useRoute,useRouter}from'vue-router';import{agentApi}from'@/api/agent';
const route=useRoute(),router=useRouter(),conversations=ref<any[]>([]),active=ref<any>(null),prompt=ref(''),streaming=ref(false),draft=ref(''),tools=ref<any[]>([]),citation=ref<any>(null),messagesEl=ref<HTMLElement>(),useFavorites=ref(false);let connection:any
const quick=['总结今天 AI 行业','分析这个事件的核心变化','比较主要观点','预测后续趋势'];
async function loadList(){conversations.value=await agentApi.list()}
async function open(id:string){active.value=await agentApi.detail(id);await router.replace({query:{conversation:id}});await scroll()}
async function create(){const c=await agentApi.create({title:'新会话',event_id:route.query.event_id||null,context_config:{source:route.query.event_id?'event':'manual'}});await loadList();await open(c.id)}
async function ensure(){const id=String(route.query.conversation||'');if(id)await open(id);else await create()}
async function send(){const text=prompt.value.trim();if(!text||streaming.value)return;active.value.messages.push({id:Date.now(),role:'user',content:text,status:'completed',citations:[]});prompt.value='';draft.value='';tools.value=[];streaming.value=true;connection=agentApi.send(active.value.id,{content:text,context_additions:useFavorites.value?[{type:'favorite'}]:[],knowledge_base_ids:[]},async(t,d)=>{if(t==='message.delta')draft.value+=d.text;if(t==='tool.started')tools.value.push({...d,status:'running'});if(t==='tool.completed'){const x=[...tools.value].reverse().find((v:any)=>v.name===d.name);if(x)Object.assign(x,d,{status:'completed'})}if(t==='message.completed'){active.value.messages.push(d);draft.value='';streaming.value=false;await loadList()}if(t==='message.failed'){draft.value=d.message;streaming.value=false}await scroll()})}
function stop(){connection?.close();streaming.value=false;draft.value=''}async function scroll(){await nextTick();messagesEl.value?.scrollTo({top:messagesEl.value.scrollHeight})}async function remove(id:string){await agentApi.remove(id);if(active.value?.id===id)active.value=null;await loadList();if(!active.value)await create()}onMounted(async()=>{await loadList();await ensure()});onBeforeUnmount(stop)
</script>
<template><main class="agent-page"><aside><header><strong>InfoPulse Agent</strong><button title="新建会话" @click="create">＋</button></header><input placeholder="搜索会话"/><nav><button v-for="c in conversations" :key="c.id" :class="{active:active?.id===c.id}" @click="open(c.id)"><span>{{c.title}}</span><small>{{c.event_id?'固定事件':'自由对话'}}</small><i @click.stop="remove(c.id)">×</i></button></nav></aside><section class="chat"><header><div><small>AI AGENT</small><h1>{{active?.title||'新会话'}}</h1></div><span v-if="active?.event_id">事件上下文已固定</span></header><div ref="messagesEl" class="messages"><section v-if="!active?.messages?.length" class="welcome"><h2>今天需要分析什么？</h2><div><button v-for="q in quick" :key="q" @click="prompt=q">{{q}}</button></div></section><article v-for="m in active?.messages" :key="m.id" :class="m.role"><div><small>{{m.role==='user'?'YOU':'INFOPULSE'}}</small><p>{{m.content}}</p><footer v-if="m.citations?.length"><button v-for="c in m.citations" :key="c.id" @click="citation=c">来源 · {{c.source}}</button></footer><div v-if="m.role==='assistant'" class="feedback"><button @click="agentApi.feedback(active.id,m.id,'up')">有帮助</button><button @click="agentApi.feedback(active.id,m.id,'down')">需改进</button></div></div></article><div v-if="tools.length" class="tools"><div v-for="t in tools" :key="t.name"><span>{{t.status==='completed'?'✓':'…'}}</span>{{t.name==='search'?'正在搜索公开情报':'正在读取事件上下文'}}<small v-if="t.count!==undefined">{{t.count}} 条证据</small></div></div><article v-if="draft" class="assistant"><div><small>INFOPULSE · STREAMING</small><p>{{draft}}</p></div></article></div><footer class="composer"><textarea v-model="prompt" rows="2" placeholder="输入问题，Agent 会展示搜索、读取与引用过程" @keydown.ctrl.enter="send"></textarea><div><span><label><input v-model="useFavorites" type="checkbox"/> 收藏上下文</label> · 自动搜索 · 知识库尚未启用</span><button v-if="streaming" @click="stop">停止</button><button v-else @click="send">发送</button></div></footer></section><el-drawer v-model="citation" title="回答引用" size="min(480px,92%)"><template v-if="citation"><h3>{{citation.title}}</h3><blockquote>{{citation.quote}}</blockquote><p>{{citation.source}}</p><a :href="citation.url" target="_blank">打开原文</a></template></el-drawer></main></template>
<style scoped>.agent-page{height:calc(100vh - 56px);display:grid;grid-template-columns:250px 1fr;background:#f3f5f4;color:#17231f}.agent-page>aside{padding:18px 12px;border-right:1px solid #d4ddda;background:#fff}.agent-page>aside header{display:flex;justify-content:space-between;align-items:center;padding:0 6px 16px}.agent-page>aside header button{width:30px;height:30px;border:1px solid #ccd7d2;background:white}.agent-page>aside>input{width:100%;height:36px;padding:0 9px;border:1px solid #d4ddda}.agent-page nav{margin-top:12px;display:grid;gap:4px}.agent-page nav button{position:relative;padding:10px;border:0;border-radius:5px;display:grid;text-align:left;background:transparent}.agent-page nav button.active{background:#e8f2ef}.agent-page nav small{color:#7a8b85}.agent-page nav i{position:absolute;right:8px;top:10px}.chat{min-width:0;display:grid;grid-template-rows:68px 1fr auto}.chat>header{padding:0 28px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid #d4ddda;background:#fff}.chat h1{margin:3px 0;font-size:19px}.chat header small{color:#188178}.messages{overflow:auto;padding:28px max(20px,calc((100% - 840px)/2))}.messages article{margin-bottom:18px;display:flex}.messages article>div{max-width:780px;padding:16px 18px;border:1px solid #d4ddda;border-radius:7px;background:white}.messages article.user{justify-content:flex-end}.messages article.user>div{color:white;background:#19302a}.messages p{white-space:pre-wrap;line-height:1.7}.messages footer button,.feedback button{margin-right:6px;border:0;color:#16776e;background:#e8f3f0}.feedback{margin-top:12px}.tools{margin:10px 0;padding:12px;border-left:2px solid #178078;background:#edf4f2}.tools div{padding:5px}.tools span{margin-right:8px}.tools small{margin-left:8px}.welcome{padding-top:12vh;text-align:center}.welcome h2{font:650 34px Georgia,serif}.welcome div{display:flex;flex-wrap:wrap;justify-content:center;gap:8px}.welcome button{padding:10px;border:1px solid #ccd7d2;background:white}.composer{margin:0 auto 18px;width:min(840px,calc(100% - 28px));padding:10px;border:1px solid #bfcfc9;border-radius:7px;background:white}.composer textarea{width:100%;resize:none;border:0;outline:0}.composer>div{display:flex;justify-content:space-between;color:#84938e;font-size:11px}.composer button{padding:7px 13px;border:0;border-radius:4px;color:white;background:#17312a}blockquote{padding:14px;border-left:3px solid #178078;background:#edf4f2}@media(max-width:720px){.agent-page{height:calc(100vh - 116px);grid-template-columns:1fr}.agent-page>aside{display:none}.chat>header{padding:0 16px}.messages{padding:18px 12px}.welcome h2{font-size:27px}}</style>
