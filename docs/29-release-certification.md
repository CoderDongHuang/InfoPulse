# 阶段 29 发布认证记录

## 认证状态

当前候选状态：**RC 验证中，GA 未签署**。本地自动门禁通过不等于生产 GA；生产基础设施、真实支付/OAuth、值班演练和责任人签署必须在隔离试点环境完成。

2026-08-05 本地认证证据：后端 154 项测试通过；前端生产构建通过；API 冻结快照包含 353 条路径；PostgreSQL 16 + pgvector 从空库升级、回滚至阶段 28、再升级至阶段 29 通过；500 请求、25 并发健康检查 0 失败，P95 310.18ms。真实浏览器完成注册、登录和阶段 20 至 28 九个工作区巡检，并修复 Element Plus 组件未注册问题。

## 自动化证据

| 门禁 | 命令 | 通过条件 |
| --- | --- | --- |
| API 契约 | `python scripts/api_contract_check.py` | 冻结快照无未评审变化 |
| 后端全量回归 | `python -m unittest discover -s tests -v` | 全部测试通过，外部模型禁用 |
| 数据库迁移 | `alembic upgrade head` | 单一 head、全部迁移成功 |
| PostgreSQL 演练 | `docker compose up -d postgres redis` 后执行迁移和负载脚本 | pgvector 可用、迁移和校验通过 |
| 前端构建 | `npm run build` | 类型检查及生产打包通过 |
| 阶段 20-28 E2E | 真实浏览器访问对应工作区并检查控制台/网络 | 关键数据可见、无失败请求和运行时错误 |

## 生产签署项

- 安全：OAuth、密钥轮换、租户隔离、依赖与镜像扫描。
- 可靠性：容量、SLO、错误预算、值班、备份恢复和多区域演练。
- 财务：计费、税务、托管、保险、国库、赔付逐笔对账。
- 合规：隐私、数据驻留、监管规则包和公共利益审计。
- 产品：隔离试点安装、升级、回滚、业务验收及 P0/P1 清零。

上述五项由责任人签署后才允许创建 GA 标签。当前仓库不自动伪造签署或外部演练结果。
