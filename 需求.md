# InfoPulse 架构设计说明书（最终版）

> **项目定位**：AI 增强型前后端分离 SaaS 应用 —— 融合实时爬虫技术与大模型工作流的垂直领域舆情洞察平台。
> **文档版本**：v3.3
> **最后更新**：2026-07-05

---

## 一、项目概述与产品定位

### 1.1 一句话简介

InfoPulse 是一个基于 Vue 3 + FastAPI 的全栈 AI 舆情洞察平台，通过实时爬虫抓取 + 大模型工作流分析，为用户提供水军识别、情绪文案生成和热点事件结构化梳理三大核心能力。

### 1.2 核心差异化

| 对比维度 | 传统 RAG 项目 | 纯 Agent 项目 | **InfoPulse** |
|---|---|---|---|
| 数据来源 | 用户提前上传知识库 | AI 自行决定 | **实时全网爬虫抓取** |
| 控制模式 | 检索 → 生成 | AI 自主规划步骤 | **开发者定义 Workflow，AI 负责分析** |
| 产品形态 | 问答对话框 | 终端命令行 | **商业级 SaaS 前后端分离应用** |
| 工程壁垒 | 低（套壳） | 中（不稳定） | **高（爬虫 + 清洗 + Prompt 工程 + 全栈）** |

### 1.3 三大核心模块

| 模块 | 英文路由 | 一句话描述 |
|---|---|---|
| 避坑雷达 | `/anti-scam` | 多平台水军识别 + 真实痛点提炼 |
| 互联网嘴替 | `/mouthpiece` | 情绪共鸣 + 爆款文案生成 |
| 吃瓜时间线 | `/timeline` | 碎片信息结构化 + 事件时间轴梳理 |

---

## 二、核心风险与对策（⚠️ 决定项目成败的魔鬼细节）

本章节记录了所有"纯技术文档不会写，但不处理项目就跑不起来"的隐性风险。

### 2.1 重型爬虫（Playwright）的三大风险

本项目采用 Playwright 应对小红书等强反爬平台，在带来数据权威性的同时，引入了以下特定风险：

| 风险维度 | 具体表现 | **应对策略（必须严格执行）** |
|---|---|---|
| **1. 资源消耗巨大** | Playwright 启动一个 Chromium 浏览器实例至少占用 500MB-1GB 内存。用户并发一高，服务器直接 OOM 崩溃。 | ① **单例模式**：整个应用只启动一个浏览器实例，所有请求共用，用完不关，减少重复开销。<br>② **Redis 队列串行化**：如果 A 用户正在抓取，B 用户必须排队等待，绝不允许多个浏览器同时运行。<br>③ **内存上限**：Docker 或服务器配置中设置内存硬限制，防止 OOM 拖垮整机。 |
| **2. 部署极其困难** | 本地跑得好好的，一上云（Docker / Serverless）就报错 "Browser not found" 或权限被拒。 | ① **放弃 Serverless**：不要用 Vercel / Hugging Face Spaces 等免费平台跑后端爬虫，直接上**云服务器（ECS/CVM）** 或 **Docker 容器**。<br>② **Dockerfile 预装依赖**：必须写 `RUN playwright install-deps` 和 `RUN playwright install chromium`，否则 Chromium 跑不起来。<br>③ **本地验证先行**：先在本地 Docker 环境完整跑通爬虫流程，确认没问题再上云。 |
| **3. 反爬封禁风险** | 小红书/抖音的反爬是动态演进的。今天能抓，明天可能就加了新的验证码或指纹检测，代码直接报废。 | ① **人机协同**：代码中检测到验证码弹窗时，通过 SSE 通知前端弹出交互层，让用户手动完成滑块验证，绝不死循环重试。<br>② **浏览器指纹伪装**：集成 `playwright-stealth` 插件，抹除自动化特征（`navigator.webdriver` 等），伪装成真实用户。<br>③ **频率严格随机化**：每次请求间隔 2-5 秒随机，绝不固定间隔。<br>④ **User-Agent 轮换池**：维护至少 10 个真实浏览器 UA，每次请求随机取一个。 |

> **一句忠告**：用重型爬虫是为了数据的权威性，但代价是运维成本极高。你要做好心理准备——这个项目后期可能需要花几十块钱买个云服务器，或者在本地开个机子挂着跑。

### 2.2 大模型依赖风险

| 风险 | 对策 |
|---|---|
| LLM API 调用失败或超时 | 前端显示"重试"按钮 + 错误提示；后端设置 60s 超时，超时自动降级返回部分结果 |
| Token 消耗过大 | 爬虫原始文本先经过**本地清洗压缩**（去重、截断），再送入 LLM，节省 Token |
| 模型输出格式不稳定 | 使用 LangChain 的 `PydanticOutputParser` 强校验输出格式，解析失败自动重试一次 |

---

## 三、非功能性需求

这部分是写给面试官或投资人看的，证明你有大局观，不只是"把功能做出来"。

### 3.1 性能指标

| 指标 | 目标值 | 说明 |
|---|---|---|
| 首屏加载时间 | < 2s | 通过 CDN + 静态资源压缩保证 |
| 单次分析响应时间 | < 60s | 含爬虫耗时，允许较长等待，但通过 SSE 实时推送进度 |
| SSE 流式首字节延迟 | < 1s | 用户点击分析后，1 秒内必须收到第一个进度事件 |
| API 接口响应（非爬虫） | < 200ms | 登录、查询历史等普通接口 |
| 数据库查询 | < 50ms | 单表查询，复杂查询走索引 |

### 3.2 并发与可用性

| 指标 | 策略 |
|---|---|
| 爬虫任务并发 | 基于 Redis 队列的**串行处理**，同一时刻最多 1 个爬虫任务在执行，其余排队 |
| 普通请求并发 | FastAPI async 支持，预估单机可承载 100+ 并发普通请求 |
| 系统可用性 | 目标 99.5%（允许因反爬策略调整导致的计划内维护） |

### 3.3 数据隐私与合规（重要）

- **数据用途声明**：用户输入的关键词和生成的文案，仅用于本次分析，**绝不**存储于数据库用于任何其他商业用途（除非用户主动点击"收藏"）。
- **爬虫合规提示**：前端在用户点击"开始分析"前，展示提示——"数据来源于互联网公开信息，仅供个人参考"。
- **密码安全**：用户密码使用 bcrypt（cost factor=12）哈希存储，绝不存明文。
- **日志脱敏**：应用日志中不得记录用户的完整手机号、密码、Token 等敏感信息。

---

## 四、错误处理与降级策略

系统不可能不报错。当某个环节挂掉时，系统必须有"体面的退路"。

### 4.1 爬虫降级

| 场景 | 降级策略 |
|---|---|
| 小红书抓取失败（验证码/封禁/超时） | 自动跳过小红书，只抓知乎和微博，前端提示："小红书暂时不可用，已为您切换备用数据源" |
| 所有平台均抓取失败 | 返回错误状态 + 提示用户："当前网络环境异常，请稍后重试"；**不要白屏** |
| 单个平台返回空结果 | 正常展示，前端标注该平台"未获取到相关数据" |

### 4.2 AI 服务降级

| 场景 | 降级策略 |
|---|---|
| LLM API 超时（>60s） | 前端显示"AI 响应超时，请点击重试"按钮；后端终止本次请求，不阻塞队列 |
| LLM 返回格式无法解析 | 后端自动重试一次（携带格式错误信息）；两次都失败则返回原始爬虫数据 + 错误提示 |
| LLM API Key 耗尽/欠费 | 后端健康检查接口返回 `llm_status: unavailable`，前端全局提示"AI 服务暂不可用" |

### 4.3 前端错误处理

| 场景 | 处理方式 |
|---|---|
| 网络断开 | Axios 拦截器全局捕获，显示 "网络连接异常" Toast |
| 401 未登录 | 自动跳转登录页，并记住当前页面路径（登录后跳回） |
| 500 服务器错误 | 显示 "服务器开小差了，请稍后重试" + 重试按钮 |
| SSE 连接中断 | 自动重连（最多 3 次），失败后提示用户手动重试 |

---

## 五、核心技术栈与版本锁定

### 5.1 前端

| 技术 | 版本 | 用途 |
|---|---|---|
| Node.js | `v18.17.0` / `v20.10.0` (LTS) | 运行时环境 |
| Vue 3 | `^3.4.0` | 核心框架 |
| Vite | `^5.0.0` | 构建工具 |
| Vue Router | `^4.2.0` | 前端路由 |
| Pinia | `^2.1.0` | 全局状态管理 |
| Element Plus | `^2.5.0` | UI 组件库 |
| Axios | `^1.6.0` | HTTP 请求封装 |

### 5.2 后端

| 技术 | 版本 | 用途 |
|---|---|---|
| Python | `3.10.x` / `3.11.x`（**禁用 3.12+**） | 运行时环境 |
| FastAPI | `^0.109.0` | 异步 Web 框架 |
| Uvicorn | `^0.27.0` | ASGI 服务器 |
| Pydantic | `^2.5.0` | 数据校验 |
| LangChain | `^0.1.0` | AI 调度与 Prompt 管理 |
| SQLAlchemy | `^2.0` | ORM 数据库操作 |
| Alembic | `^1.13` | 数据库迁移 |
| Requests | `^2.31.0` | 轻量网页抓取 |
| Playwright | `^1.41.0` | 强反爬平台浏览器自动化 |
| playwright-stealth | `^1.0.6` | 浏览器指纹伪装 |
| Redis | `^5.0` | 缓存与任务队列 |

---

## 六、项目目录结构

### 6.1 前端目录结构

```
frontend/
├── public/
│   └── favicon.ico
├── src/
│   ├── assets/                    # 静态资源（Logo、图片、字体）
│   │   ├── logo.png
│   │   └── styles/
│   │       ├── variables.css      # CSS 变量（色彩系统）
│   │       ├── global.css         # 全局样式
│   │       └── transitions.css    # 路由过渡动画
│   ├── components/                # 全局复用组件
│   │   ├── layout/
│   │   │   ├── AppHeader.vue      # 顶部导航栏
│   │   │   ├── AppFooter.vue      # 底部状态栏
│   │   │   └── AppLayout.vue      # 全局布局容器
│   │   ├── common/
│   │   │   ├── LoadingOverlay.vue  # 全局加载蒙层
│   │   │   ├── SseTextStream.vue   # 流式文本渲染组件
│   │   │   ├── EmptyState.vue      # 空状态占位图
│   │   │   ├── ErrorRetry.vue      # 错误重试组件
│   │   │   └── CaptchaModal.vue    # 手动验证码弹窗
│   │   └── auth/
│   │       └── LoginCard.vue       # 登录/注册卡片
│   ├── views/                     # 页面视图
│   │   ├── HomeView.vue           # 首页
│   │   ├── AuthView.vue           # 登录/注册页
│   │   ├── anti-scam/
│   │   │   └── AntiScamView.vue   # 避坑雷达
│   │   ├── mouthpiece/
│   │   │   └── MouthpieceView.vue # 互联网嘴替
│   │   └── timeline/
│   │       └── TimelineView.vue   # 吃瓜时间线
│   ├── router/
│   │   └── index.ts               # 路由配置
│   ├── stores/                    # Pinia 状态管理
│   │   ├── user.ts                # 用户登录状态
│   │   ├── app.ts                 # 全局配置（主题、加载态）
│   │   └── history.ts             # 历史记录
│   ├── api/                       # API 请求封装
│   │   ├── request.ts             # Axios 实例 + 拦截器
│   │   ├── auth.ts                # 认证相关接口
│   │   ├── antiScam.ts            # 避坑雷达接口
│   │   ├── mouthpiece.ts          # 嘴替生成接口
│   │   └── timeline.ts            # 吃瓜时间线接口
│   ├── utils/                     # 工具函数
│   │   ├── sse.ts                 # SSE 流式连接工具
│   │   └── clipboard.ts           # 剪贴板工具
│   ├── App.vue
│   └── main.ts
├── index.html
├── vite.config.ts
├── tsconfig.json
└── package.json
```

### 6.2 后端目录结构

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                    # FastAPI 应用入口
│   ├── config.py                  # 配置管理（从 .env 读取）
│   ├── dependencies.py            # 依赖注入（get_current_user 等）
│   ├── models/                    # SQLAlchemy 数据模型
│   │   ├── __init__.py
│   │   ├── user.py                # 用户表
│   │   ├── analysis_history.py    # 分析历史记录表
│   │   └── saved_result.py        # 收藏结果表
│   ├── schemas/                   # Pydantic 请求/响应模型
│   │   ├── __init__.py
│   │   ├── auth.py                # 登录/注册 Schema
│   │   ├── anti_scam.py           # 避坑雷达 Schema
│   │   ├── mouthpiece.py          # 嘴替生成 Schema
│   │   └── timeline.py            # 吃瓜时间线 Schema
│   ├── api/                       # 路由层（仅处理 HTTP 请求/响应）
│   │   ├── __init__.py
│   │   ├── auth.py                # /api/v1/auth/*
│   │   ├── anti_scam.py           # /api/v1/anti-scam/*
│   │   ├── mouthpiece.py          # /api/v1/mouthpiece/*
│   │   └── timeline.py            # /api/v1/timeline/*
│   ├── services/                  # 业务逻辑层
│   │   ├── __init__.py
│   │   ├── auth_service.py        # 鉴权逻辑
│   │   ├── crawler/               # 爬虫模块
│   │   │   ├── __init__.py
│   │   │   ├── base.py            # 爬虫抽象基类（策略模式）
│   │   │   ├── browser_manager.py # Playwright 浏览器单例管理器
│   │   │   ├── stealth_patcher.py # playwright-stealth 集成
│   │   │   ├── xiaohongshu.py     # 小红书爬虫（Playwright）
│   │   │   ├── weibo.py           # 微博爬虫
│   │   │   └── zhihu.py           # 知乎爬虫（httpx）
│   │   ├── analyzer/              # AI 分析模块
│   │   │   ├── __init__.py
│   │   │   ├── anti_scam.py       # 水军识别 + 痛点提炼
│   │   │   ├── mouthpiece.py      # 文案生成
│   │   │   └── timeline.py        # 时间线梳理
│   │   ├── cleaner.py             # 数据清洗器（去重、去噪、截断）
│   │   └── prompts/               # Prompt 模板管理
│   │       ├── __init__.py
│   │       ├── loader.py          # Prompt 文件热加载器
│   │       └── templates/         # Prompt 模板文件（.txt，可直接编辑）
│   │           ├── anti_scam_system.txt
│   │           ├── anti_scam_user.txt
│   │           ├── mouthpiece_system.txt
│   │           ├── mouthpiece_user.txt
│   │           ├── timeline_system.txt
│   │           └── timeline_user.txt
│   ├── core/                      # 基础设施
│   │   ├── __init__.py
│   │   ├── database.py            # 数据库连接 + Session
│   │   ├── security.py            # JWT 生成/校验
│   │   ├── redis.py               # Redis 连接 + 爬虫任务队列
│   │   └── llm.py                 # LLM 客户端封装
│   └── middleware/                 # 中间件
│       ├── __init__.py
│       └── cors.py                # CORS 配置
├── alembic/                       # 数据库迁移
│   └── versions/
├── tests/                         # 测试
│   ├── test_auth.py
│   ├── test_anti_scam.py
│   └── test_mouthpiece.py
├── Dockerfile                     # Docker 镜像（含 Chromium 依赖）
├── docker-compose.yml             # 一键启动前后端 + DB + Redis
├── requirements.txt
├── .env.example
└── alembic.ini
```

---

## 七、数据库设计

### 7.1 ER 图概要

```
┌─────────────┐       ┌──────────────────────┐       ┌──────────────────┐
│    users    │──1:N──│  analysis_histories   │──1:N──│  saved_results   │
└─────────────┘       └──────────────────────┘       └──────────────────┘
```

### 7.2 用户表 `users`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | UUID (PK) | 用户唯一标识 |
| `username` | VARCHAR(50), UNIQUE | 用户名 |
| `email` | VARCHAR(100), UNIQUE | 邮箱 |
| `password_hash` | VARCHAR(255) | bcrypt 哈希密码 |
| `avatar_url` | VARCHAR(500) | 头像地址 |
| `is_active` | BOOLEAN | 是否激活 |
| `created_at` | DATETIME | 注册时间 |
| `updated_at` | DATETIME | 最后更新时间 |

### 7.3 分析历史记录表 `analysis_histories`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | UUID (PK) | 记录唯一标识 |
| `user_id` | UUID (FK → users) | 所属用户 |
| `module` | VARCHAR(20) | 模块名：anti_scam / mouthpiece / timeline |
| `input_params` | JSON | 用户输入的参数（关键词、平台、场景等） |
| `output_result` | JSON | AI 分析后的结构化结果 |
| `status` | VARCHAR(20) | pending / running / completed / failed |
| `error_message` | TEXT | 失败时的错误信息（用于调试和前端展示） |
| `created_at` | DATETIME | 创建时间 |

### 7.4 收藏结果表 `saved_results`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | UUID (PK) | 收藏唯一标识 |
| `user_id` | UUID (FK → users) | 所属用户 |
| `history_id` | UUID (FK → analysis_histories) | 关联的分析记录 |
| `note` | VARCHAR(200) | 用户备注 |
| `created_at` | DATETIME | 收藏时间 |

---

## 八、API 接口设计

### 8.1 通用约定

- Base URL：`/api/v1`
- 认证方式：`Authorization: Bearer <JWT_TOKEN>`
- 流式响应：SSE（Server-Sent Events），`Content-Type: text/event-stream`
- 响应格式：
  ```json
  {
    "code": 0,
    "message": "success",
    "data": { ... }
  }
  ```

### 8.2 认证模块

| 方法 | 路径 | 说明 | 认证 |
|---|---|---|---|
| POST | `/api/v1/auth/register` | 用户注册 | 否 |
| POST | `/api/v1/auth/login` | 用户登录，返回 JWT | 否 |
| POST | `/api/v1/auth/refresh` | 刷新 Token | 是 |
| GET | `/api/v1/auth/me` | 获取当前用户信息 | 是 |
| PUT | `/api/v1/auth/me` | 更新用户信息 | 是 |

### 8.3 系统模块

| 方法 | 路径 | 说明 | 认证 |
|---|---|---|---|
| GET | `/api/v1/health` | 健康检查（含 LLM 状态、爬虫状态） | 否 |

### 8.4 避坑雷达

| 方法 | 路径 | 说明 | 认证 |
|---|---|---|---|
| POST | `/api/v1/anti-scam/analyze` | 发起分析（SSE 流式返回） | 是 |
| GET | `/api/v1/anti-scam/history` | 查询历史分析记录 | 是 |
| GET | `/api/v1/anti-scam/history/{id}` | 查看单条记录详情 | 是 |
| DELETE | `/api/v1/anti-scam/history/{id}` | 删除历史记录 | 是 |

**请求体 (POST /analyze)**：
```json
{
  "keyword": "某品牌手机",
  "platforms": ["xiaohongshu", "weibo", "zhihu"],
  "max_items": 50
}
```

**SSE 流式事件类型**：

| 事件 | 说明 | 示例 |
|---|---|---|
| `progress` | 爬取进度 | `{"stage": "crawling", "platform": "xiaohongshu", "current": 20, "total": 50}` |
| `warning` | 非致命警告（如某平台降级） | `{"platform": "xiaohongshu", "message": "小红书暂时不可用，已切换备用源"}` |
| `captcha` | 需要手动验证码 | `{"platform": "xiaohongshu", "image_base64": "..."}` |
| `chunk` | AI 分析结果片段 | `"根据分析，该产品存在..."` |
| `result` | 最终结构化结果 | `{"risk_score": 85, ...}` |
| `error` | 致命错误 | `{"code": "CRAWLER_FAILED", "message": "所有平台抓取失败"}` |

### 8.5 互联网嘴替

| 方法 | 路径 | 说明 | 认证 |
|---|---|---|---|
| POST | `/api/v1/mouthpiece/generate` | 生成文案（SSE 流式返回） | 是 |
| GET | `/api/v1/mouthpiece/history` | 查询生成历史 | 是 |
| GET | `/api/v1/mouthpiece/history/{id}` | 查看单条记录 | 是 |

**请求体 (POST /generate)**：
```json
{
  "scenario": "职场吐槽",
  "emotion": "humorous",
  "emotion_intensity": 7,
  "content": "老板让我周末加班，还不给加班费"
}
```

### 8.6 吃瓜时间线

| 方法 | 路径 | 说明 | 认证 |
|---|---|---|---|
| POST | `/api/v1/timeline/generate` | 生成时间线（SSE 流式返回） | 是 |
| GET | `/api/v1/timeline/history` | 查询历史 | 是 |
| GET | `/api/v1/timeline/history/{id}` | 查看单条记录 | 是 |

**请求体 (POST /generate)**：
```json
{
  "event_name": "某某明星塌房事件",
  "background": "（可选）用户补充的背景信息",
  "verify_sources": true
}
```

---

## 九、认证鉴权方案

- **方案**：JWT (JSON Web Token)
- **Access Token**：有效期 30 分钟，存入前端内存（Pinia），不持久化到 localStorage（防 XSS）
- **Refresh Token**：有效期 7 天，存入 httpOnly Secure Cookie，用于静默刷新 Access Token
- **密码加密**：bcrypt，cost factor = 12
- **前端路由守卫**：`router.beforeEach` 检查 Pinia 中的登录状态，未登录跳转 `/auth`，同时记住目标路径（登录后跳回）

---

## 十、全局 UI 视觉与交互设计规范

### 10.1 色彩系统

```css
:root {
  /* 品牌色 */
  --color-primary: #409EFF;        /* Element Plus 原生蓝 —— 科技与信任 */
  --color-primary-light: #66b1ff;
  --color-primary-dark: #3a8ee6;

  /* 辅助色 */
  --color-accent: #6B46C1;         /* 科技紫 —— 用于强调与点缀 */

  /* 背景色 */
  --bg-page: #f5f7fa;              /* 页面底色，浅灰护眼 */
  --bg-card: rgba(255, 255, 255, 0.85);  /* 毛玻璃卡片背景 */

  /* 文字色 */
  --text-primary: #303133;         /* 主标题 */
  --text-regular: #606266;         /* 正文 */
  --text-secondary: #909399;       /* 辅助说明 */

  /* 边框 */
  --border-color: #dcdfe6;

  /* 状态色 */
  --color-success: #67c23a;
  --color-warning: #e6a23c;
  --color-danger: #f56c6c;
}
```

### 10.2 设计风格

- **核心风格**：现代极简 + 毛玻璃（Glassmorphism）
  - 卡片：`background: rgba(255,255,255,0.85); backdrop-filter: blur(12px); border-radius: 12px;`
  - 阴影：`box-shadow: 0 4px 20px rgba(0,0,0,0.08);`
- **字体**：系统默认字体栈（`-apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif`）
- **圆角**：全局 8px，卡片 12px，按钮 6px

### 10.3 交互规范

| 场景 | 交互方式 |
|---|---|
| 按钮点击 | `transform: scale(0.95)` 微缩放 + Loading 状态 |
| AI 文本生成 | **SSE 流式输出**，模拟打字机效果 |
| 页面切换 | Vue Router `fade` 过渡动画（300ms） |
| 卡片悬停 | `translateY(-4px)` + 阴影加深 |
| 导航项悬停 | `translateY(-2px)` + 颜色变为 `--color-primary` |
| 全局请求 | 顶部 2px 加载进度条 |
| 空状态 | 插画 + 引导文案，避免空白页面 |
| 错误状态 | 错误图标 + 描述 + "重试" 按钮 |

---

## 十一、导航栏与全局布局设计

### 11.1 导航栏结构

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ [Logo] InfoPulse          首页   避坑雷达▾   嘴替生成▾   吃瓜时间线▾   [铃铛][头像▾] │
└──────────────────────────────────────────────────────────────────────────────┘
```

- **左侧**：系统 Logo（40×40px）+ 产品名 "InfoPulse"
- **中间**：一级导航菜单（并排式），每项带下拉子菜单
- **右侧**：
  - 未登录：[登录] [注册]（注册按钮使用主色调）
  - 已登录：通知铃铛图标 + 用户头像 + 下拉菜单（个人中心 / 历史记录 / 设置 / 退出登录）

### 11.2 下拉子菜单结构

| 一级菜单 | 下拉子菜单 |
|---|---|
| 首页 | 产品介绍 / 使用指南 / 案例展示 |
| 避坑雷达 ▾ | 商品评测 / 品牌对比 / 风险预警 |
| 嘴替生成 ▾ | 职场吐槽 / 情感分享 / 生活趣事 |
| 吃瓜时间线 ▾ | 热点追踪 / 事件回顾 / 趋势分析 |

### 11.3 导航交互细节

- **激活态**：当前页面导航项底部显示 2px 主色调下划线
- **悬停态**：上下浮动 2px + 颜色过渡到主色调
- **下拉菜单**：悬停展开，毛玻璃背景，`opacity` + `translateY` 过渡动画
- **响应式**：屏幕宽度 < 768px 时，导航折叠为汉堡菜单

### 11.4 全局布局

```
┌─────────────────────────────────────────────────┐
│                   顶部导航栏 (sticky)              │
├─────────────────────────────────────────────────┤
│                                                  │
│                   <router-view>                  │
│                   主内容区                        │
│                  (min-height: calc(100vh - 120px))│
│                                                  │
├─────────────────────────────────────────────────┤
│  底部状态栏：面包屑导航 ｜ 系统时间 ｜ 网络状态 ｜ © InfoPulse │
└─────────────────────────────────────────────────┘
```

---

## 十二、页面级设计详述

### 12.1 首页 `/`

**定位**：产品门面，吸引并引导用户。

**内容结构**：
1. **Hero 区域**：Slogan "穿透信息迷雾，洞察真实情绪" + CTA 按钮 "立即体验"（跳转避坑雷达）
2. **三大功能卡片**（CSS Grid 3 列）：
   - 避坑雷达：图标 + 简介 "多平台水军识别，买到真正好货"
   - 嘴替生成：图标 + 简介 "AI 帮你把心里话，说成爆款"
   - 吃瓜时间线：图标 + 简介 "碎片信息结构化，吃瓜吃明白"
3. **底部**：简单的统计数据（已分析 XX 条内容，已生成 XX 篇文案）

### 12.2 登录/注册页 `/auth`

**定位**：用户鉴权入口。

**内容结构**：
- 居中毛玻璃登录卡片（400px 宽）
- 账号 / 密码输入框 + 登录按钮
- 底部 "还没有账号？立即注册" 切换链接
- 可选：第三方登录图标（GitHub）

**状态栏**：完全隐藏，保持登录页沉浸感。

### 12.3 避坑雷达 `/anti-scam`

**定位**：多源数据抓取 → 水军识别 → 真实痛点输出。

**布局**：左侧输入配置区（30%）+ 右侧结果展示区（70%）。

**左侧输入区**：
- 商品/事件关键词输入框
- 抓取平台多选框（小红书、微博、知乎）
- 抓取数量滑块（10-100 条）
- "开始排雷" 主按钮（点击后显示加载动画）

**右侧展示区**：
- **风险仪表盘**：环形进度条，显示 "水军疑似度"（如：⚠️ 高危 85%）
- **痛点标签云**：彩色 Tag 展示真实黑点（`掉电快` `售后差` `虚假宣传`）
- **证据列表**：折叠面板，每条水军评论附判定理由和信息源
- **操作日志面板**：底部折叠区域，展示爬取进度日志（含平台降级提示）

### 12.4 互联网嘴替 `/mouthpiece`

**定位**：情绪共鸣 → 爆款文案生成。

**布局**：左侧输入配置区（30%）+ 右侧预览区（70%）。

**左侧输入区**：
- 场景选择下拉框（职场吐槽 / 相亲分享 / 探店测评 / 生活趣事）
- 情绪风格选择（阴阳怪气 / 幽默自黑 / 温柔发疯 / 犀利毒舌）
- 情绪强度滑块（1-10）
- 大白话经历文本域（"把你想说的都写下来..."）
- "注入灵魂" 主按钮

**右侧展示区**：
- **文案预览卡片**：模拟小红书 UI 样式
  - 标题区域（加粗大字）
  - 正文区域（自然分段 + Emoji 点缀）
  - Tags 标签行
- **操作栏**：一键复制全文 / 一键复制标签 / 重新生成 / 导出图片

### 12.5 吃瓜时间线 `/timeline`

**定位**：碎片信息结构化 → 客观时间轴。

**布局**：左侧输入区（30%）+ 右侧时间轴展示区（70%）。

**左侧输入区**：
- 热点事件名称输入框
- 补充背景信息文本域（可选）
- 来源验证开关（开启后要求 AI 附信息源链接）
- "梳理时间线" 主按钮

**右侧展示区**：
- **垂直时间轴**（Element Plus Timeline 组件）：
  - 左侧：时间节点
  - 右侧：客观事件描述（去情绪化、纯事实）
- **信息源链接**：每个节点下方附原始链接，可一键跳转
- **导出按钮**：导出为图片 / 复制为 Markdown

---

## 十三、AI Prompt 工程设计

### 13.1 Prompt 工程管理规范（重要）

> **核心理念**：Prompt 是代码的一部分，必须用管理代码的方式来管理 Prompt。

| 管理维度 | 规范 |
|---|---|
| **存储方式** | 所有 Prompt 模板独立存放为 `.txt` 文件，路径：`backend/app/services/prompts/templates/` |
| **文件命名** | `{模块名}_{角色}.txt`，如 `anti_scam_system.txt`、`anti_scam_user.txt` |
| **变量语法** | 使用 Python `str.format()` 或 Jinja2 `{{ variable }}` 语法注入动态参数 |
| **热加载** | Prompt 加载器每次请求时从文件读取，改 Prompt 不需要重启服务 |
| **版本控制** | `.txt` 文件纳入 Git，修改 Prompt 有 commit 记录，可追溯可回滚 |

### 13.2 通用设计原则

- **Chain 模式**：每个模块采用 LangChain 的 `LLMChain`，串联 Prompt 模板 + LLM 调用 + 输出解析
- **结构化输出**：使用 LangChain 的 `PydanticOutputParser`，确保 LLM 返回可解析的 JSON；解析失败自动重试一次
- **流式输出**：使用 FastAPI `StreamingResponse` + LangChain 的 `stream()` 方法
- **本地预处理**：爬虫原始文本在送入 LLM 前，先经过 `cleaner.py` 进行去重、截断、去噪，减少 Token 消耗

### 13.3 避坑雷达 Prompt 设计

**核心 Chain**：爬虫文本输入 → 水军识别 → 痛点提炼 → 结构化输出

**System Prompt 要点**：
- 角色：你是一个资深的消费者权益保护专家，擅长识别水军评论和虚假宣传
- 水军特征定义：模板化语言、过度使用感叹号、缺乏具体使用细节、时间集中在短时间段内、账号信息异常
- 输出格式：JSON，包含 `risk_score`（0-100）、`water_army_comments`（附判定理由）、`real_pain_points`（痛点标签数组）

### 13.4 嘴替生成 Prompt 设计

**核心 Chain**：用户原始输入 → 情绪注入 → 风格改写 → 平台优化

**System Prompt 要点**：
- 角色：你是一个具有强烈共情能力的顶级新媒体文案写手，擅长小红书/微博爆款文风
- 风格字典：根据 `emotion` 参数（`sarcastic` / `humorous` / `gentle_crazy` / `sharp`）调用对应的写作风格指令
- 输出要求：标题（≤20 字，抓眼球）、正文（3-5 段，口语化 + Emoji）、Tags（5-8 个）

### 13.5 吃瓜时间线 Prompt 设计

**核心 Chain**：碎片文本输入 → 事件提取 → 时间排序 → 去情绪化改写 → 结构化输出

**System Prompt 要点**：
- 角色：你是一个专业的事件调查记者，擅长从碎片信息中还原事件全貌
- 核心指令：去情绪化——将带有主观情绪的描述改写为客观事实陈述
- 输出格式：JSON 数组，每个元素包含 `time`、`title`、`description`、`sources`

---

## 十四、爬虫模块设计

### 14.1 架构模式

采用**策略模式（Strategy Pattern）** + **浏览器单例模式**，定义统一的爬虫抽象基类：

```python
# 抽象基类
class BaseCrawler(ABC):
    @abstractmethod
    async def search(self, keyword: str, max_items: int) -> List[RawPost]:
        """根据关键词搜索帖子"""

    @abstractmethod
    async def get_comments(self, post_id: str, max_items: int) -> List[RawComment]:
        """获取帖子的评论列表"""

    @abstractmethod
    def is_available(self) -> bool:
        """检查该平台爬虫当前是否可用"""
```

### 14.2 浏览器单例管理器（Playwright 专用）

```python
class BrowserManager:
    """全局浏览器单例管理器 —— 整个应用只启动一个 Chromium 实例"""
    _instance = None

    def __new__(cls):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
            cls._instance._browser = None
        return cls._instance

    async def get_browser(self) -> Browser:
        if self._browser is None or not self._browser.is_connected():
            self._browser = await playwright.chromium.launch(
                headless=True,
                args=["--no-sandbox", "--disable-setuid-sandbox"]
            )
        return self._browser
```

### 14.3 任务队列机制

```
用户 A 请求 → Redis Queue → [BrowserManager 单例] → 抓取 → 返回 → 释放
用户 B 请求 → Redis Queue → [等待 A 完成...] → [BrowserManager 复用] → 抓取 → 返回
```

- 使用 Redis List 作为任务队列（`LPUSH` + `BRPOP`）
- 同一时刻最多 1 个 Playwright 任务在执行
- 排队超过 120s 自动超时，返回提示 "当前排队人数较多，请稍后重试"

### 14.4 平台适配策略

| 平台 | 爬虫类型 | 技术方案 | 代码位置 | 难度 |
|---|---|---|---|---|
| 知乎 | 轻量爬虫 | `httpx` + `BeautifulSoup`，API 抓取为主 | 主仓库 `crawler/zhihu.py` | 低 |
| 微博 | 轻量/重量混合 | `requests` 优先，遇验证码自动降级 | 主仓库 `crawler/weibo.py` | 中 |
| 小红书 | 重量爬虫 | `Playwright` + `playwright-stealth` | **不在主仓库。** 以接口 + 独立下载包的形式提供 | 高 |

### 14.5 小红书爬虫的"物理隔离"设计（⚠️ 法律合规关键）

> **核心原则**：主仓库（GitHub）只包含框架和接口定义。小红书的实际爬取代码**不放在主仓库**，以此保护你的 GitHub 账号免受 DMCA 律师函攻击。

**主仓库提供的内容**（安全，不会触发 DMCA）：

```python
# backend/app/services/crawler/base.py —— 主仓库代码

class BaseCrawler(ABC):
    """所有爬虫必须实现的接口"""
    platform_name: str

    @abstractmethod
    async def search(self, keyword: str, max_items: int) -> List[RawPost]:
        """搜索帖子"""

    @abstractmethod
    async def get_comments(self, post_id: str, max_items: int) -> List[RawComment]:
        """获取评论"""

    @abstractmethod
    def is_available(self) -> bool:
        """检查当前是否可用"""


# backend/app/services/crawler/registry.py —— 主仓库代码

CRAWLER_REGISTRY: Dict[str, Type[BaseCrawler]] = {
    "zhihu": ZhihuCrawler,
    "weibo": WeiboCrawler,
    # "xiaohongshu": None,  # ← 用户需自行下载并放入 crawler/plugins/ 目录
}


# backend/app/services/crawler/plugins/README.md —— 主仓库代码

"""
第三方爬虫插件目录

本目录默认为空。要启用小红书等平台的数据源：
1. 从 [独立下载链接] 获取插件文件
2. 将 .py 文件放入本目录
3. 在 registry.py 中取消对应注释
4. 重启服务

⚠️ 注意：插件代码不在本仓库的授权范围内，请自行评估使用风险。
"""
```

**独立渠道提供的内容**（与主仓库物理隔离）：

- 小红书爬虫插件放在一个**独立的 Gist、网盘链接或私有仓库**中
- 用户需要主动去该链接下载 `xiaohongshu_plugin.py`，放到 `crawler/plugins/` 目录
- 主仓库的 `.gitignore` 中排除 `crawler/plugins/*.py`

**为什么这么做**：

| 场景 | 主仓库直接放 `.py` | **物理隔离** |
|---|---|---|
| 小红书法务看到你的 GitHub | 代码里有 `goto("xiaohongshu.com")` → DMCA 举报 → 仓库被删 + 账号被封 | 仓库代码只有接口定义 → 无法构成 DMCA 举报理由 → 仓库安全 |
| 用户想用小红书功能 | 直接运行 | 需要多一步手动下载，但你的仓库不会被律师函干掉 |
| 法律定性 | "提供侵权工具" | **"提供通用框架，用户自行扩展"** → 这就是 GPL/LGPL 的武器化用法 |

### 14.7 插件发现体验（用户的第一印象）

> **问题**：用户 `docker-compose up` 后发现避坑雷达提示"插件未安装"，却不知道去哪找插件——兴奋感瞬间变成挫败感。

**✅ 三层引导，确保用户不迷路**：

| 层级 | 位置 | 内容 |
|---|---|---|
| **启动日志** | 后端启动时检测 `plugins/` 目录 | 若为空，打印：`[InfoPulse] 第三方数据源插件为空。下载链接：https://xxx（含安装教程）` |
| **前端页面** | 避坑雷达输入区底部 | 默认平台多选框中，小红书选项旁显示 `🔌 需安装插件` 链接，点击弹出安装教程弹窗 |
| **错误提示** | 用户勾选小红书但未安装插件 | 不报错，直接弹窗："小红书插件未安装，点击查看 30 秒安装教程 →" |

**前端插件引导示例**：

```
┌────────────────────────────────────────────┐
│  抓取平台：                                  │
│  ☑ 知乎   ☑ 微博   ☐ 小红书 🔌 需要插件       │
│                                              │
│  点击"🔌 需要插件" → 弹出：                    │
│  ┌──────────────────────────────────────┐    │
│  │  📦 安装小红书数据源插件               │    │
│  │                                      │    │
│  │  1. 下载插件文件（点击下方按钮）         │    │
│  │  2. 放入 crawler/plugins/ 目录        │    │
│  │  3. 重启 docker-compose              │    │
│  │                                      │    │
│  │  [📥 下载插件]  [📖 详细教程]           │    │
│  └──────────────────────────────────────┘    │
└────────────────────────────────────────────┘
```

### 14.6 反爬对抗完整措施

- `playwright-stealth` 插件：自动抹除 `navigator.webdriver`、`chrome.runtime` 等自动化特征
- User-Agent 轮换池（≥10 个真实浏览器 UA，每次请求随机取）
- 请求频率随机化（2-5 秒随机间隔，模拟真人浏览节奏）
- Cookie 持久化与自动刷新（文件存储，跨请求复用）
- 检测到验证码 → SSE 推送 `captcha` 事件 → 前端弹窗 → 用户手动完成 → 继续抓取
- 单平台失败自动重试 3 次（间隔指数递增），3 次均失败则降级跳过

---

## 十五、数据流架构

### 15.1 完整数据流（含降级路径）

```
用户输入关键词
    │
    ▼
┌─────────────────┐
│  FastAPI Router  │ ← 参数校验 (Pydantic) + JWT 鉴权
└────────┬────────┘
         │
    ┌────▼────┐
    │ Service  │ ← 业务编排层（含降级决策）
    └────┬────┘
         │
    ┌────▼────────────────────────┐
    │  Crawler Dispatcher         │ ← 根据 platforms 参数派发爬虫
    │  ┌──────┐ ┌──────┐ ┌──────┐│
    │  │ 知乎  │ │ 微博  │ │小红书 ││  ← 并行抓取（asyncio.gather）
    │  │httpx  │ │混合  │ │PW单例││
    │  └──┬───┘ └──┬───┘ └──┬───┘│
    │    │        │        │     │
    │    │    失败/降级 → SSE warning 事件 → 前端提示  │
    └────┼────────┼────────┼─────┘
         │        │        │
         └────────┼────────┘
                  ▼
         ┌────────────────┐
         │  Data Cleaner  │ ← 去重、去噪、HTML 标签清洗、智能截断（保留完整句子）
         └───────┬────────┘
                 │
         ┌───────▼────────┐
         │  LLM Analyzer   │ ← Prompt 模板 + LangChain Chain + stream()
         │  (60s 超时)     │
         └───────┬────────┘
                 │  失败 → SSE error 事件 → 前端 "重试" 按钮
         ┌───────▼────────┐
         │  SSE Stream     │ ← 流式事件：progress → warning? → chunk... → result
         │  + DB Persist   │ ← 结果异步写入 analysis_histories
         └───────┬────────┘
                 │
                 ▼
         ┌────────────────┐
         │  Vue 3 前端     │ ← EventSource API 解析 → 打字机渲染 → 卡片展示
         └────────────────┘
```

### 15.2 SSE 流式输出时序

```
Client                          Server
  │                                │
  │── POST /analyze ──────────────▶│
  │                                │── 加入 Redis 队列
  │◀── event: progress ────────────│   "queued: 前面还有 2 人"
  │◀── event: progress ────────────│   "crawling: 知乎 20/50"
  │◀── event: warning ─────────────│   "小红书暂时不可用，已切换备用源"
  │◀── event: progress ────────────│   "crawling: 微博 50/50"
  │◀── event: progress ────────────│   "analyzing: AI 分析中..."
  │◀── event: chunk ───────────────│   "根据分析..."
  │◀── event: chunk ───────────────│   "该产品存在..."
  │◀── event: result ──────────────│   {完整 JSON: risk_score, pain_points, ...}
  │                                │── 异步写入数据库
```

---

## 十六、部署方案（双轨策略）

InfoPulse 采用 **"开源完整版 + 免费演示站"** 双轨部署策略，兼顾技术沉淀与产品展示。

```
┌─────────────────────────────────────────────────────────────────┐
│                        双轨部署策略                               │
│                                                                 │
│  轨道一：GitHub 开源（完整版）          轨道二：免费演示站（轻量版）    │
│  ┌──────────────────────────┐       ┌──────────────────────────┐ │
│  │ 用户 docker-compose up   │       │ Render + Vercel + Supabase│ │
│  │ 本地一键启动全栈          │       │ 全免费栈，公网域名访问      │ │
│  │ 含 Playwright 完整爬虫    │       │ 仅含嘴替 + 吃瓜时间线     │ │
│  │ 零运维成本，零法律风险     │       │ 引导用户去 GitHub 下完整版 │ │
│  └──────────────────────────┘       └──────────────────────────┘ │
│         ↑                                   ↑                   │
│    技术名片 · 沉淀硬核能力              产品门面 · 吸引用户眼球        │
└─────────────────────────────────────────────────────────────────┘
```

### 16.1 轨道一：GitHub 开源完整版（强烈推荐作为主要分发方式）

**核心逻辑**：代码在用户自己的电脑上运行，爬虫走用户自己的网络环境。你只提供"工具"，用户对自己如何使用工具负责。零运维成本，零法律风险。

**用户使用流程**：

```bash
# 1. 克隆仓库
git clone https://github.com/你的用户名/InfoPulse.git
cd InfoPulse

# 2. 配置环境变量（填入自己的 LLM API Key）
cp .env.example .env
# 编辑 .env，填入 LLM_API_KEY=sk-xxxxx

# 3. 一键启动
docker-compose up -d

# 4. 浏览器打开
# http://localhost:5173
```

**GitHub 仓库结构要求**：

```
InfoPulse/                        # GitHub 仓库根目录
├── frontend/                     # Vue 3 前端源码
├── backend/                      # FastAPI 后端源码
├── docker-compose.yml            # 一键启动（核心卖点）
├── Dockerfile                    # 后端镜像（含 Chromium）
├── Dockerfile.frontend           # 前端镜像
├── .env.example                  # 环境变量模板
├── README.md                     # 精美的项目说明（见下文）
├── README_zh.md                  # 中文版 README
├── LICENSE                       # MIT License
└── docs/
    ├── screenshots/              # 产品截图（README 里要用）
    │   ├── home.png
    │   ├── anti-scam.png
    │   ├── mouthpiece.png
    │   └── timeline.png
    └── architecture.md           # 架构设计说明书（本文档）
```

**README.md 必备内容**：

| 板块 | 内容 |
|---|---|
| 项目 Logo + 标语 | "穿透信息迷雾，洞察真实情绪" |
| 产品截图 | 4 张关键页面的 GIF/PNG，直观展示 |
| 一键启动 | `docker-compose up -d` 命令 + 动图演示 |
| 核心功能 | 三大模块简介 + 技术亮点 |
| 技术栈 | 前端/后端/AI/爬虫 技术标签墙 |
| 快速开始 | 前置要求（Docker、LLM API Key）+ 3 步启动 |
| 项目结构 | 目录树速览 |
| Star History | 放一张 Star 增长曲线图（等有人气了） |

**docker-compose.yml 完整设计**：

```yaml
version: "3.8"

services:
  # ========== 数据库 ==========
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: infopulse
      POSTGRES_PASSWORD: infopulse
      POSTGRES_DB: infopulse
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U infopulse"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ========== 缓存 & 任务队列 ==========
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ========== 后端 (FastAPI + Playwright) ==========
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql+asyncpg://infopulse:infopulse@db:5432/infopulse
      REDIS_URL: redis://redis:6379/0
      LLM_API_KEY: ${LLM_API_KEY}
      LLM_API_BASE: ${LLM_API_BASE:-https://api.openai.com/v1}
      LLM_MODEL: ${LLM_MODEL:-gpt-4o-mini}
      CRAWLER_HEADLESS: "true"
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:-change-me-in-production}
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  # ========== 前端 (Vue 3) ==========
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.frontend
    ports:
      - "5173:80"
    depends_on:
      - backend

volumes:
  pgdata:
```

### 16.2 轨道二：免费轻量演示站（吸引用户，展示门面）

**核心逻辑**：部署一个"阉割版"到免费平台，只保留不需要爬虫的功能（嘴替生成 + 吃瓜时间线）。用户看到精美的前端界面后，自然想去 GitHub 下载完整版体验避坑雷达。

**功能拆分策略**：

| 功能 | 演示站（免费平台） | 本地完整版（GitHub） |
|---|---|---|
| 首页 + 登录注册 | ✅ 完整 | ✅ 完整 |
| 嘴替生成 | ✅ 完整（纯 LLM，无需爬虫） | ✅ 完整 |
| 吃瓜时间线 | ✅ 完整（纯 LLM，无需爬虫） | ✅ 完整 |
| 避坑雷达 | ❌ 灰显 + 引导文案 | ✅ 完整（含 Playwright 爬虫） |
| 爬虫模块 | ❌ 不部署 | ✅ 完整 |

**避坑雷达在演示站的展示方式**：

```
┌───────────────────────────────────────────────┐
│                                                │
│            🛡️  避坑雷达                         │
│                                                │
│     ┌─────────────────────────────┐            │
│     │                             │            │
│     │  此功能需要本地运行爬虫引擎      │            │
│     │  演示站暂不可用                │            │
│     │                             │            │
│     │    ┌───────────────────┐    │            │
│     │    │ 前往 GitHub 下载源码  │    │            │
│     │    └───────────────────┘    │            │
│     │   docker-compose up -d      │            │
│     │   一键启动完整版              │            │
│     └─────────────────────────────┘            │
│                                                │
│   💡 完整版支持：小红书 · 微博 · 知乎 多平台水军识别  │
│                                                │
└───────────────────────────────────────────────┘
```

**演示站部署方案**：

| 组件 | 部署平台 | 费用 | 说明 |
|---|---|---|---|
| 前端 | **Vercel** | 免费 | 连接 GitHub 仓库，推送即自动部署，自带 `*.vercel.app` 域名 |
| 后端 | **Render**（Web Service） | 免费 | 原生支持 Docker 部署，512MB 内存 + 不杀后台进程，完美适合轻量 FastAPI（无 Chromium）。自带 `*.onrender.com` 域名。**注意：不要用 HF Spaces，它检测到浏览器自动化特征会直接 Kill 容器。** |
| 数据库 | **Supabase**（免费层） | 免费 | 托管 PostgreSQL，500MB 存储，足够演示站使用 |
| 缓存 | **Upstash**（免费层） | 免费 | 托管 Redis，足够轻量任务队列 |

> **为什么不把完整版也部署到公网？**
> 因为 Playwright 启动一个 Chromium 就要 500MB+ 内存，公网部署需要买云服务器（最低配也要大几十块/月），而且多用户并发时极易 OOM。开源出去，用户的电脑就是免费的服务器——这才是最聪明的做法。

**演示站环境变量（轻量版）**：

```env
# 演示站 .env（部署到 Render）
# 数据库 —— 使用 Supabase 免费托管 PostgreSQL
DATABASE_URL=postgresql+asyncpg://xxx:xxx@db.xxxxx.supabase.co:5432/postgres

# Redis —— 使用 Upstash 免费托管 Redis
REDIS_URL=redis://xxx:xxx@xxx.upstash.io:6379

# JWT
JWT_SECRET_KEY=your-production-secret
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# LLM —— 用户需要自带 Key，或你提供一个限额 Key
LLM_API_KEY=sk-xxxxxxxx
LLM_API_BASE=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# 爬虫 —— 演示站禁用
CRAWLER_ENABLED=false

# 演示模式 —— 前端显示 GitHub 引导
DEMO_MODE=true
GITHUB_REPO_URL=https://github.com/你的用户名/InfoPulse
```

### 16.3 两种方案对比总结

| 维度 | 轨道一：GitHub 开源 | 轨道二：免费演示站 |
|---|---|---|
| **目标用户** | 有动手能力的开发者 | 想快速体验的普通用户 |
| **功能完整度** | 100%（含爬虫） | ~60%（无爬虫模块） |
| **部署难度** | 用户只需 3 行命令 | 你一次性配置好 |
| **运维成本** | 零（用户自己跑） | 零（全免费平台） |
| **法律风险** | 零（代码在用户电脑跑） | 极低（无爬虫功能） |
| **核心价值** | 技术名片 + GitHub 涨粉 | 产品门面 + 引流到 GitHub |
| **域名** | 不需要（本地 localhost） | 免费子域名（Vercel + Render） |

### 16.4 最终建议

**两个都做。** 这是目前开源 AI 产品的最优打法：

- 演示站上的醒目标语：*"本站为轻量演示版，完整功能（含多平台避坑雷达）请前往 GitHub 下载源码，docker-compose up -d 一键启动！"*
- 演示站负责帮你"拉新"，GitHub 负责帮你"沉淀"。两者互相导流，形成闭环。

---

## 十七、现实世界的生存指南（避坑指南）

> **本章定位**：前面所有章节是"理想状态下的完美蓝图"。本章是"当代码跑在真实机器上时会遇到的所有烂事，以及怎么解决"。

### 17.1 为什么 Hugging Face Spaces 跑不了你的后端

**问题**：很多教程推荐用 HF Spaces 部署 AI 应用，但对 InfoPulse 来说这是**死路一条**。

| 原因 | 详细说明 |
|---|---|
| **浏览器自动化被禁** | HF Spaces 的容器守护进程会检测 `chromium` / `google-chrome` 进程。一旦发现，立即 Kill 容器，没有任何警告。 |
| **共享内存限制** | 即使不跑爬虫，`playwright` 这个 Python 包只要被 import，就会触发依赖库加载。HF 的 `/dev/shm` 只有 64MB，Chromium 需要至少 256MB。 |
| **端口模型不兼容** | HF Spaces 只暴露一个端口（7860），你的 docker-compose 需要同时跑 db + redis + backend + frontend 四个端口，架构上就跑不通。 |

**✅ 正确做法**：
- **后端轻量版** → **Render**（Web Service 免费层，512MB RAM，支持自定义 Dockerfile，不杀后台进程）
- **数据库** → **Supabase**（免费 PostgreSQL，公网 IP，前端 Vercel 和后端 Render 都能连）
- **缓存** → **Upstash**（免费 Redis，公网 IP）
- **前端** → **Vercel**（免费，自动部署）

**关键点**：演示站的每个组件都有独立的公网地址，不存在"Docker 内部网络不通"的问题。

### 17.2 Playwright 的真实资源消耗（不是你以为的 500MB）

**问题**：文档里写的"500MB-1GB"是理想情况。现实中远不止。

| 真实场景 | 表现 | 后果 |
|---|---|---|
| **内存泄漏** | 浏览器实例每完成一次爬取任务，不会释放全部内存。跑 10 次任务后，内存从 500MB 涨到 2GB+。 | 用户电脑风扇狂转，系统卡顿，最终 OOM Kill。 |
| **僵尸进程** | `chromium` 主进程结束后，`--type=renderer` / `--type=gpu-process` 等子进程可能不退出。 | 32 个僵尸进程挂在后台，每个占 50-100MB。 |
| **CPU 占满** | Chromium 解析 JS 渲染页面时，单个任务就能把 2 核 CPU 吃满。 | 用户以为是病毒，直接关机。 |
| **Docker 的 `/dev/shm`** | Chromium 默认使用 `/dev/shm`（共享内存），Docker 默认只分配 64MB。页面复杂时直接崩溃。 |

**✅ 正确做法（必须在代码中实现）**：

```python
# backend/app/services/crawler/browser_manager.py

import psutil
import os

class BrowserManager:
    _MEMORY_LIMIT_MB = int(os.getenv("BROWSER_RESTART_MB", "800"))  # 熔断阈值（可通过环境变量调整）
    _TASK_COUNT_RESET = 20          # 每 20 个任务强制重启浏览器

    def __init__(self):
        self._task_count = 0

    async def _check_health(self):
        """每次任务前体检"""
        # 1. 内存熔断
        mem = psutil.Process(os.getpid()).memory_info().rss / 1024 / 1024
        if mem > self._MEMORY_LIMIT_MB:
            await self._restart_browser()

        # 2. 任务计数重启
        self._task_count += 1
        if self._task_count >= self._TASK_COUNT_RESET:
            await self._restart_browser()
            self._task_count = 0

    async def _restart_browser(self):
        """强制重启浏览器 + 清理僵尸进程"""
        if self._browser:
            await self._browser.close()
        # 清理所有残留的 chromium 子进程
        for proc in psutil.process_iter(['pid', 'name']):
            if 'chromium' in proc.info['name'].lower():
                try:
                    proc.kill()
                except:
                    pass
        self._browser = await self._launch_browser()
```

**Chrome 启动参数（必须加的）**：

```python
await playwright.chromium.launch(
    headless=True,
    args=[
        "--no-sandbox",               # Docker 内必需（安全代价）
        "--disable-setuid-sandbox",   # 同上
        "--disable-dev-shm-usage",    # 不用 /dev/shm，改用 /tmp
        "--disable-gpu",              # 无头模式不需要 GPU
        "--single-process",           # 减少子进程数（牺牲性能，省内存）
        "--max_old_space_size=512",   # V8 堆上限
    ]
)
```

### 17.3 Docker 镜像选型：别用 Alpine

**问题**：很多人为了镜像小，写 `FROM python:3.11-alpine`。Alpine 使用 musl libc，而 Playwright 下载的 Chromium 是动态链接 glibc 的。

| 基础镜像 | 大小 | Chromium 兼容性 | 最终选择 |
|---|---|---|---|
| `python:3.11-alpine` | ~50MB | ❌ 几乎跑不起来 | 别用 |
| `python:3.11-slim` | ~150MB | ⚠️ 缺字体、缺系统库 | 不推荐 |
| **`python:3.11-slim-bookworm`** | ~150MB | ✅ 装 `playwright install-deps` 后完美运行 | ✅ 选这个 |
| `ubuntu:22.04` | ~300MB | ✅ 最稳 | 如果 slim 有问题再切这个 |

**✅ Dockerfile 关键片段**：

```dockerfile
FROM python:3.11-slim-bookworm

# Playwright 系统依赖（这一步大约增加 200MB）
RUN apt-get update && apt-get install -y \
    libnss3 libnspr4 libatk-bridge2.0-0 libdrm2 libxkbcommon0 \
    libxcomposite1 libxdamage1 libxrandr2 libgbm1 libpango-1.0-0 \
    libcairo2 libasound2 libatspi2.0-0 \
    && rm -rf /var/lib/apt/lists/*

# 安装 Python 依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 安装 Chromium（这一步下载 ~150MB）
RUN playwright install chromium
RUN playwright install-deps chromium
```

### 17.4 Windows 用户的 Docker 噩梦

**问题**：国内 Windows 用户装 Docker Desktop 经常失败——WSL2 装不上、BIOS 没开虚拟化、Hyper-V 冲突、镜像拉取被墙。

**✅ 降级方案——提供纯 Python 运行方式**：

```bash
# 不用 Docker 的苦手用户，走这个流程
# 前置要求：Python 3.11、Node.js 18、PostgreSQL 本地安装

# 后端
cd backend
python -m venv venv
source venv/bin/activate        # Windows: venv\Scripts\activate
pip install -r requirements.txt
playwright install chromium
playwright install-deps chromium
uvicorn app.main:app --reload

# 前端（新终端）
cd frontend
npm install
npm run dev
```

**在 README 里写清楚**：

> **推荐配置**：2 核 CPU / 4GB 以上内存 / 支持 Docker 的操作系统
> **最低配置**：2 核 CPU / 2GB 内存（仅限纯 Python 运行方式，爬虫任务期间会卡顿）
>
> ⚠️ **不要在 1 核 / 1GB 内存的机器上运行完整版**。Chromium 启动即需要 ~500MB，加上 Python 后端和 PostgreSQL，1GB 内存必定 OOM。

### 17.5 法律风险的"物理隔离"（DMCA 防护升级）

**问题**：GitHub 仓库里的代码是公开的。如果你的主仓库里出现了 `await page.goto("https://www.xiaohongshu.com")` 这行代码，一旦项目被平台方的法务盯上，一封 DMCA 律师函就能让 GitHub 把你的仓库删掉、甚至封你账号。

**❌ "文档洁癖"已经是过去式了**——光改措辞不够，因为 DMCA 审核看的是**代码行为**，不是你的 README 措辞。只要代码里有对目标平台的直接访问逻辑，就能被认定为"规避技术保护措施"。

**✅ 升级策略：物理隔离（"武器化" GPL 思路）**

| 位置 | ❌ 旧做法（仍可能被 DMCA） | ✅ 新做法（物理隔离） |
|---|---|---|
| 主仓库 | 放 `_xiaohongshu.py`（加下划线） | **完全不放**。主仓库只有 `BaseCrawler` 接口 + `crawler/plugins/README.md` |
| `.gitignore` | 无 | 排除 `crawler/plugins/*.py`，确保即使本地有也不会被推上去 |
| 小红书代码 | 主仓库内 | **独立 Gist / 网盘 / 私有仓库**。用户自行下载，放到 `plugins/` 目录 |
| README 话术 | "适配不同平台" | "本项目是一个通用数据分析框架。第三方平台的数据源插件由社区维护，不在本仓库授权范围内" |

**`crawler/plugins/README.md` 模板**（放在主仓库里，保护你的）:

```markdown
# 第三方爬虫插件目录

本目录默认为空。InfoPulse 是一个通用舆情分析框架，不内置任何特定平台的数据抓取逻辑。

## 如何添加数据源

1. 从插件维护者处获取 `.py` 文件
2. 将文件放入本目录
3. 在 `crawler/registry.py` 中注册该爬虫
4. 重启服务

## ⚠️ 重要提示

- 本框架不对任何第三方插件的合法性负责
- 请在使用前确认你遵守了目标平台的 robots.txt 和服务条款
- 插件代码的授权、维护和安全由插件作者独立负责
```

### 17.6 Docker 的真实资源消耗与硬件门槛

**问题**：文档里之前写的"2 核 4G 推荐"对于 Windows/macOS 上的 Docker Desktop 用户来说仍然太低。

**真实内存开销（逐层叠加）**:

```
Docker Desktop / WSL2 虚拟机基础开销    ~1.5GB
PostgreSQL 容器（空闲）                  ~100MB
Redis 容器（空闲）                       ~30MB
FastAPI 后端（空闲，无爬虫）              ~200MB
Playwright + Chromium（启动后）          ~700MB
Vue 前端开发服务器                        ~150MB
─────────────────────────────────────────────────
合计（空闲状态）                          ~2.7GB
爬虫任务进行中（Chromium 解析 JS）        额外 +500MB~1GB
─────────────────────────────────────────────────
峰值                                      ~3.7GB
```

**✅ 更新后的硬件要求**:

> | 配置等级 | CPU | 内存 | 适用场景 | 体验 |
> |---|---|---|---|---|
> | **不推荐** | 2 核 | 4GB | 纯 Python 运行，单任务 | ⚠️ 卡顿，可能 OOM |
> | **最低配置** | 4 核 | 8GB | Docker 运行，单用户 | ✅ 基本流畅 |
> | **推荐配置** | 4 核+ | 16GB | Docker 运行，多任务 | ✅ 流畅 |
>
> ⚠️ **如果你用的是 Windows 或 macOS + Docker Desktop**，上述内存需求额外 +2GB（WSL2/虚拟机开销）。

**✅ docker-compose.yml 中的资源硬限制**:

```yaml
services:
  backend:
    # ... 其他配置 ...
    deploy:
      resources:
        limits:
          memory: 2g          # 硬限制：超过 2GB 直接 Kill
        reservations:
          memory: 512m        # 预留：保证至少拿到 512MB
    # 环境变量中暗示 JVM 类似行为，通知代码做内存检查
    environment:
      MEMORY_LIMIT_MB: 1800   # 代码层熔断阈值（略低于容器上限）
```

**✅ 宿主机开启 Swap（防止 OOM 直接杀进程）**:

在 README 的 Quick Start 中增加：

```bash
# Linux 宿主机：开启 4GB Swap（防止 Docker 内存不足导致进程被杀）
sudo fallocate -l 4G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
# 永久生效
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
```

Windows/macOS 用户 Docker Desktop 默认自带 Swap，无需手动配置。

### 17.7 SSE（流式输出）的"假死"焦虑

**问题**：用户点按钮后转圈 30 秒没有任何反馈，会以为卡死了，疯狂点重试 → 队列积压更严重。

**✅ 后端 SSE 心跳**:

```python
# backend/app/api/anti_scam.py
import asyncio

async def analyze_sse(request: AnalyzeRequest, user_id: str):
    async def event_generator():
        # 1. 立即返回排队状态
        queue_position = await redis.llen("crawler:queue")
        estimated_wait = queue_position * 25  # 每个任务大约 25 秒
        yield f"event: progress\ndata: {json.dumps({'stage': 'queued', 'position': queue_position, 'estimated_wait_s': estimated_wait})}\n\n"

        # 2. 启动心跳协程
        async def heartbeat():
            while True:
                await asyncio.sleep(5)
                yield f"event: ping\ndata: heartbeat\n\n"

        # 3. 主任务 + 心跳 并行
        # ... 爬虫 + LLM 逻辑，每 5 秒若无数据则自动发 ping

    return StreamingResponse(event_generator(), media_type="text/event-stream")
```

**✅ 前端 SSE 心跳处理**:

```typescript
// frontend/src/utils/sse.ts
const TIMEOUT_MS = 30000;  // 30 秒无任何事件才判定断连
let lastEventTime = Date.now();

eventSource.addEventListener('ping', () => {
  lastEventTime = Date.now();  // 心跳重置计时器
});

eventSource.addEventListener('progress', (e) => {
  lastEventTime = Date.now();
  const data = JSON.parse(e.data);
  if (data.stage === 'queued') {
    // 显示预计等待时间
    showToast(`前面还有 ${data.position} 人，预计等待 ${data.estimated_wait_s} 秒`);
  }
});

// 超时检测
setInterval(() => {
  if (Date.now() - lastEventTime > TIMEOUT_MS) {
    eventSource.close();
    showError('连接超时，请检查网络后重试');
  }
}, 5000);
```

### 17.8 Render 免费层的"512MB 极限生存"

**问题**：Render 免费层只有 512MB RAM。FastAPI + Python 依赖 + LLM 流式缓冲区 = 很容易触顶重启。

**✅ Render 专用瘦身方案**:

```dockerfile
# backend/Dockerfile.render —— 演示站专用镜像（不含 Playwright）
FROM python:3.11-slim-bookworm

# 只装轻量依赖（不装 Playwright 和任何浏览器相关包）
COPY requirements.render.txt .
RUN pip install --no-cache-dir -r requirements.render.txt

COPY . .
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

```txt
# backend/requirements.render.txt —— 演示站专用（不含 playwright）
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.0
sqlalchemy==2.0.0
asyncpg==0.29.0
redis==5.0.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
httpx==0.26.0
langchain==0.1.0
openai==1.7.0
# 注意：这里不包含 playwright！
```

**没有 `playwright` 这个包**，FastAPI 启动内存能省 50-80MB。

**✅ 代码中优雅降级**:

```python
# backend/app/services/crawler/__init__.py
try:
    from .xiaohongshu import XiaohongshuCrawler
    CRAWLER_REGISTRY["xiaohongshu"] = XiaohongshuCrawler
except ImportError:
    # 演示站没装 Playwright，自动跳过
    pass
```

### 17.9 README 顶部免责声明模板

> 以下是必须放在 README.md **最顶部**（第一条可见内容）的免责声明：

```markdown
## ⚠️ 免责声明（使用前必读）

**本项目（InfoPulse）是一个面向技术学习与研究的通用舆情分析框架。**

- 本项目**不内置**任何特定平台的数据抓取逻辑。第三方数据源插件由社区独立维护，不在本仓库授权范围内。
- 使用本代码即表示您同意：您将自行承担因使用本代码而产生的所有法律责任。作者不对任何因使用本项目代码而导致的侵权、数据泄露或其他法律后果负责。
- 在使用任何数据源前，请务必阅读并遵守目标平台的 robots.txt、服务条款及当地法律法规。
- 本项目采用 MIT 协议开源，但协议**不覆盖**第三方插件目录（`crawler/plugins/`）中的内容。

**本项目仅供学习交流，严禁用于任何商业或违法用途。**
```

---

### 17.10 反向代理与跨域问题

**问题**：演示站前端（Vercel）和后端（Render）不在同一个域名下，浏览器的 CORS 策略会拦截请求。

**✅ 后端 CORS 配置**：

```python
# backend/app/middleware/cors.py
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "http://localhost:5173",              # 本地开发
        "https://infopulse.vercel.app",       # 演示站前端
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

在 README 的 FAQ 里写：

> **Q: 我的前端部署在 Vercel，后端在本地电脑，怎么连？**
> A: 这种场景不推荐，网络不通。要么前后端都本地跑（docker-compose），要么都用公网服务。如果你确实需要，可以用 Cloudflare Tunnel 把本地后端暴露出去。

---

## 十八、开发路线图

| 阶段 | 内容 | 关键动作 | 验证标准 |
|---|---|---|---|
| **Phase 0: 环境攻坚** | Docker + Playwright 生存验证 | ① **Dockerfile 选型**：使用 `python:3.11-slim-bookworm`（非 Alpine），安装 `playwright install-deps`。<br>② 本地跑通 Playwright 脚本，确认 Chromium 能启动。<br>③ 验证 `--disable-dev-shm-usage` 等关键启动参数。<br>④ 集成 `playwright-stealth`，验证指纹伪装。<br>⑤ **熔断机制**：实现 BrowserManager 的内存监控 + 任务计数重启逻辑。 | ① `docker-compose up` 后端能启动，`docker logs` 无 chromium 报错。<br>② 连续执行 30 次爬虫任务，内存不超 1.2GB。<br>③ 终端执行 `python test_playwright.py` 成功打开百度并截图。 |
| **Phase 1: 骨架搭建** | 前端初始化 + 后端 Auth + DB | ① Vite 创建 Vue 3 项目，配好路由/Pinia/Element Plus。<br>② FastAPI 创建后端项目，分层结构就位。<br>③ 用户注册/登录接口 + JWT 鉴权完整跑通。<br>④ 前后端 CORS 联通，Axios 拦截器就位。 | 浏览器访问 `localhost:5173` 能注册、登录、看到导航栏 |
| **Phase 2: 避坑雷达** | 爬虫 + AI 分析 + 前端页面 | ① **先做知乎**（轻量 httpx 爬虫），验证完整数据流。<br>② **再做小红书**（重量 Playwright 爬虫），接入 BrowserManager 单例。<br>③ SSE 流式输出整个链路打通。<br>④ 前端结果展示页面（仪表盘 + 标签云 + 证据列表）。 | 输入关键词 → 显示爬取进度 → AI 流式输出分析结果 → 渲染完整结果页 |
| **Phase 3: 嘴替生成** | Prompt 工程 + 文案预览 | ① 编写四套情绪风格的 System Prompt 模板。<br>② SSE 流式生成文案。<br>③ 小红书风格预览卡片 + 一键复制 + 标签独立复制。 | 输入吐槽内容 → 选择风格 → 生成可发布的文案 → 一键复制 |
| **Phase 4: 吃瓜时间线** | 时间线 AI + Timeline 组件 | ① Prompt 优化：确保时间戳提取准确。<br>② Element Plus Timeline 组件渲染。<br>③ 信息源链接 + 导出功能。 | 输入热点事件名 → 生成客观时间线 → 可导出 Markdown |
| **Phase 5: 体验打磨** | 暗黑模式、响应式、错误处理 | ① 全局错误处理（网络断开、401、500）。<br>② 爬虫降级逻辑完整测试。<br>③ 暗黑模式 CSS 变量切换。<br>④ 移动端响应式适配。<br>⑤ 动画过渡优化。 | 所有异常场景有友好提示，不白屏、不死循环 |
| **Phase 6a: GitHub 开源** | docker-compose + README + License | ① 编写 `docker-compose.yml`，确保一键启动。<br>② 编写精美 README.md（含截图、GIF 动图、快速开始指南）。<br>③ 添加 MIT License。<br>④ 清理敏感信息，确认 `.env.example` 模板完整。 | 任意一台装了 Docker 的电脑执行 `docker-compose up -d` 能跑起来 |
| **Phase 6b: 轻量演示站** | Vercel + Render + Supabase | ① 前端部署到 Vercel（连接 GitHub 自动部署）。<br>② 后端**轻量版**部署到 Render（Docker，不含 Chromium，`CRAWLER_ENABLED=false`）。<br>③ Supabase 托管 PG + Upstash 托管 Redis。<br>④ 避坑雷达页面展示引导文案 + GitHub 链接。<br>⑤ 演示站显眼位置标注"完整版请前往 GitHub"。 | 公网域名访问，嘴替 + 时间线可用，避坑雷达引导下载 |

---

## 十九、附录：决策记录

| 决策项 | 结论 | 原因 |
|---|---|---|
| 产品名称 | **InfoPulse** | 信息脉搏，简洁易记 |
| 主色调 | **#409EFF** | Element Plus 原生蓝，与组件库无缝兼容 |
| 导航方案 | **并排式 + 下拉子菜单** | 内容丰富度更高，扩展性好 |
| 认证方案 | **JWT (Access + Refresh)** | 无状态、可扩展、适合前后端分离 |
| 爬虫架构 | **策略模式 + 单例模式 + Redis 队列** | 各平台独立实现，全局复用浏览器，串行化防崩溃 |
| 流式输出 | **SSE** | 比 WebSocket 更轻量，单向推送足够 |
| 数据库 | **PostgreSQL + Redis** | PG 稳定成熟，Redis 做队列和缓存 |
| Prompt 管理 | **独立 .txt 文件 + 热加载** | 改 Prompt 不需要改代码、不需要重启，可 Git 追溯 |
| Playwright 部署 | **Docker + 用户本地运行** | GitHub 开源，用户 docker-compose up 本地启动；放弃公网部署爬虫 |
| 演示站策略 | **Vercel + Render + Supabase** | 全免费平台，仅部署非爬虫功能。Render 替代 HF Spaces（后者会 Kill 浏览器进程）。全部使用公网可连的托管服务，避免 Docker 内部网络不通。 |
| 爬虫队列 | **Redis List (LPUSH/BRPOP)** | 简单可靠，支持阻塞等待和超时 |
| Docker 基础镜像 | **python:3.11-slim-bookworm** | 禁用 Alpine（musl libc 不兼容 Chromium），slim-bookworm 体积可控且依赖齐全 |
| 浏览器熔断 | **内存 > 800MB 或 20 次任务后强制重启** | 防止内存泄漏和僵尸进程拖垮系统 |
| 代码合规 | **主仓库物理隔离（接口模式）** | 小红书爬虫代码不放在 GitHub 主仓库，以独立插件形式分发。主仓库只有 BaseCrawler 接口 + plugins/README.md。GitHub 上无可被 DMCA 举报的"规避技术保护措施"代码 |
| 用户硬件要求 | **4 核 CPU / 8GB 内存最低，16GB 推荐** | Docker Desktop（Win/Mac）基础开销 ~2GB，4GB 机器必定 OOM |
| 法律免责 | **README 顶部红色免责声明** | 声明本项目为"通用舆情分析框架"，不内置任何平台抓取逻辑；第三方插件独立维护 |
| 小红书插件 | **独立 Gist/网盘分发，用户手动安装** | 物理隔离保护 GitHub 仓库。`.gitignore` 排除 `crawler/plugins/*.py` |
| Docker 资源限制 | **compose 中 backend mem_limit: 2g** | 硬限制防 OOM 拖垮整机；代码层 1800MB 熔断预警 |
| Render 演示站 | **独立 Dockerfile.render + 独立 requirements.render.txt** | 不安装 playwright Python 包，省 50-80MB 内存，512MB 免费层勉强够用 |
| SSE 心跳 | **每 5 秒一次 ping 事件 + 前端 30s 超时判死** | 防止网络波动导致用户以为"卡死了"疯狂点重试 |
| 队列预估 | **SSE 首事件即返回排队位置 + 预计等待时间** | 缓解用户等待焦虑，降低无效重试 |

---

**文档版本**：v3.3
**最后更新**：2026-07-05
